# 03 — פגיעויות אבטחה בתלויות

> 🟢 **בוצע, מוזג ונפרס לצד הלקוח ב-30/08/2026.** הענף נמחק.
> ✅ **ו-`functions/` נפרס אף הוא, 30/08/2026 בשעה 12:02.**
> **אומת מול הענן ולא לפי דיווח:** שלוש הפונקציות מדווחות זמן עדכון
> 09:02 עד 09:03 UTC, **שבע עשרה דקות אחרי המיזוג של 11:45.**
> שני קומיטים לפי פרומפט 19,
> `DOCS/PLANING/prompts/19-dependency-vulnerabilities.md`.

## מה נסגר, במספרים

**תלויות ייצור בלבד, בלי כלי הפיתוח.**

**האפליקציה: 18 יורדות ל-10.** היו 2 קריטיות ו-5 גבוהות. **לא נשארה אף
קריטית, ונשארה גבוהה אחת.**

**`functions/`: 26 יורדות ל-10.** היו 3 קריטיות ו-15 גבוהות. **נשארה קריטית
אחת וחמש גבוהות.**

ובסך הכל, כולל כלי הפיתוח, בעץ האפליקציה: **מ-49 ל-26, ובלי אף קריטית.**

## קומיט 1 — מה ש-npm יודע לתקן

`npm audit fix` בלי `--force`, בשני העצים.

**שום `package.json` לא נגע.** כל התיקונים נכנסו בטווח הגרסאות שכבר היה מוצהר,
ולכן השתנו רק שני קובצי הנעילה. **גם `functions/package.json` לא השתנה**,
בניגוד למה שהפרומפט הניח: כל התיקונים שם הם בתלויות עקיפות בלבד.

**על מסלול הייצור של האפליקציה:**

`react-router` ו-`react-router-dom` מ-7.6.3 ל-7.18.3.
`protobufjs` מ-7.5.3 ל-7.6.6.
`websocket-driver` מ-0.7.4 ל-0.7.5.
`@grpc/grpc-js` מ-1.9.15 ל-1.9.16, ועותק שני מ-1.13.4 ל-1.14.4.
`@protobufjs/utf8` מ-1.1.0 ל-1.1.2.
`i18next-http-backend` מ-3.0.2 ל-3.0.6.

**ב-`functions/`:** 25 חבילות עקיפות שונו ו-4 נוספו. בהן `websocket-driver`
ל-0.7.5, `express` מ-4.21.2 ל-4.22.2, `lodash` מ-4.17.21 ל-4.18.1,
`node-forge` מ-1.3.1 ל-1.4.0, `path-to-regexp` מ-0.1.12 ל-0.1.13, `qs`
מ-6.13.0 ל-6.15.3 ו-`tmp` מ-0.2.3 ל-0.2.7. **`firebase-admin` נשאר על 11
ו-`firebase-functions` על 4.**

**באותה פקודה נגררו גם כלי הבנייה**, כי `npm audit fix` אינו יודע להפריד בין
ייצור לפיתוח. `vite` מ-5.4.8 ל-5.4.21, `rollup` מ-4.24.0 ל-4.63.1, `postcss`
מ-8.4.47 ל-8.5.26 ומשפחת `@babel` מ-7.25 ל-7.29. **בעל המוצר אישר את זה
במפורש**, והבנייה אומתה אחרי.

## קומיט 2 — `xlsx` מהמקור של SheetJS

**מ-0.18.5 ל-0.20.3**, מהכתובת
`https://cdn.sheetjs.com/xlsx-0.20.3/xlsx-0.20.3.tgz`.

**זו השורה היחידה שהשתנתה ב-`package.json` בכל הקמפיין.**

זיהום אב הטיפוס נסגר ב-0.19.3 וה-ReDoS ב-0.20.2, כלומר 0.20.3 סוגרת את שתיהן.
**ה-API זהה ולכן שום קוד מוצר לא זז.**

### ⚠️ והמחיר, שיש לזכור אותו

**תלות שאינה ב-npm אינה נראית ל-`npm audit` ואינה מתעדכנת מעצמה.** מהיום
`xlsx` היא פריט שמישהו צריך לבדוק ידנית מול
`https://docs.sheetjs.com/docs/getting-started/installation/nodejs`.
**היא לא תופיע ברשימת הפגיעויות גם אם תתגלה בה אחת חדשה.**

**ומה שמקל אם יבוא יום והיא תצטרך להתחלף:** הספרייה בשימוש במקום אחד בלבד
ולקריאה בלבד, שתי קריאות ב-`ImportItemsModal.tsx`. אין ייצוא ואין כתיבה.

## מה נשאר, ולמי הוא שייך

**⚠️ וכאן המסמך הזה טעה עד היום.** הוא תלה את כל השארית ב-[04](04-firebase-sdk.md).
**בפועל השארית מתחלקת לשתיים, ורק חצי ממנה שייכת ל-04.**

**באפליקציה, כל עשר הפגיעויות הן שרשרת אחת:** `undici` בחומרה גבוהה, שמגיעה
דרך `@firebase/auth` וגוררת אחריה שמונה חבילות `@firebase` ואת `firebase`
עצמה. **הגרסה המותקנת 10.14.1 היא האחרונה בגרסה ראשית 10**, כלומר אין תיקון
שאינו `firebase@11`. **זו ספריית הלקוח, לא ספריית השרת, ואין לה קשר ל-04.**
נפתחה עליה [72](72-firebase-client-sdk-v11.md).

**ושם גם נבדק דבר שמוריד את הדחיפות:** `undici` נמצא מאחורי נקודת הכניסה של
Node בלבד, הבנייה של האתר בוחרת את נקודת הכניסה של הדפדפן, **והמחרוזת אינה
מופיעה אפילו פעם אחת בקובץ שנשלח למשתמש.** אף סקריפט בפרויקט אינו טוען את
הספרייה הזו ב-Node. **כלומר הקוד הפגיע אינו נשלח ואינו רץ.**

**ב-`functions/`, כל עשר הפגיעויות ממתינות ל-`firebase-admin@14`**, ובהן
הקריטית היחידה שנשארה, `protobufjs`. **זו [04](04-firebase-sdk.md).**

> ✅ **נסגר 05/09/2026 בקמפיין [04](04-firebase-sdk.md).**
>
> **⚠️ ומה שהתברר במדידה מחדש: המספר לא עמד במקום.** ביום השדרוג היו
> ב-`functions/` **13 ולא 10**, כי שלוש בינוניות חדשות נוספו מאז 30/08 על
> `express`, `body-parser` ו-`qs`. **הן נסגרו כבר בקומיט הראשון**, כי
> `firebase-functions@7` מביא איתו `express` בגרסה 5 במקום 4.
>
> **ואחרי `firebase-admin@14.3.0`: מ-10 ל-7.** נסגרו הקריטית `protobufjs`
> וכל חמש הגבוהות, `@google-cloud/firestore`, `@grpc/grpc-js`,
> `firebase-admin` עצמה, `google-gax` ו-`protobufjs-cli`.
>
> **המצב היום: 7 בינוניות, אפס גבוהות, אפס קריטיות.** ובסך הכל, מ-13 ל-7.
>
> **ובפרויקט כולו לא נשארה אף פגיעות קריטית או גבוהה בקוד השרת.**

### מה שבע הבינוניות שנשארו, ולמה אין להן תיקון

**כולן שרשרת אחת ואותה פגיעות:** `uuid`, בדיקת גבולות חסרה, שמגיעה דרך
`teeny-request` ו-`retry-request` אל `@google-cloud/storage` ומשם אל
`firebase-admin`. **שבע רשומות, פגם אחד.**

**⚠️ ו-npm מציע להן "תיקון" שאסור לקבל:** הוא מציע `firebase-admin@10.3.0`,
כלומר **הורדת גרסה בארבע גרסאות ראשיות אחורה**, מה שיחזיר את הקריטית ואת חמש
הגבוהות. **זו התנהגות ידועה של הכלי כשאין תיקון קדימה, ולא הצעה אמיתית.**

**כלומר אין כאן מה לעשות היום.** הן ממתינות לגרסה של `@google-cloud/storage`
שתישא `uuid` מתוקן, ואז הן ייסגרו מאליהן בעדכון רגיל של `firebase-admin`.
**זה פריט למעקב ולא משימה.**

## מה אומת

**הבנייה עוברת** בעץ האפליקציה, ושלושת המודולים של `functions/` נטענים.
ל-`functions/` אין שלב בנייה.

**הבנייה הקרה עוברת גם היא.** `npm ci` על מטמון ריק לגמרי, בתיקייה נקייה,
עם קובץ הנעילה בלבד: 551 חבילות ב-36 שניות, **כולל משיכת `xlsx` מהשרת של
SheetJS**, ואחריה בנייה מלאה שהצליחה. **זו הסימולציה הקרובה ביותר למה
ש-Netlify עושה.**

**מסלול קריאת הקובץ אומת על הגרסה החדשה**, מול קובץ `xlsx` אמיתי ודרך אותן
שתי קריאות שהמוצר מריץ: זיהוי שורת כותרת בעברית, שמות פריטים בעברית, תא הערות
ריק, פסילת שם קצר מדי ופסילת כמות מחוץ לטווח. **הכל התנהג כמו קודם.**

**גודל החבילה גדל ב-32 קילובייט**, מ-1,504 ל-1,537, כי 0.20.3 גדולה מ-0.18.5.

## ואומת גם על האתר החי, אחרי הפריסה

**האתר מגיש את הבנייה החדשה.** קובץ ה-JavaScript שהוחלף הורד ונבדק:
**המחרוזת של הגרסה הישנה אינה מופיעה בו כלל**, וטביעת האצבע שלו זהה לזו של
הבנייה המקומית של אותו קוד. **כלומר `xlsx` החדשה נמצאת בפועל אצל המשתמשים.**

**והניווט נבדק בפרודקשן ולא רק מקומית:** טעינה ישירה של עמוד פנימי, מעבר
בלחיצה על קישור, וכפתור אחורה של הדפדפן. **בלי שגיאות בקונסולה.**

**מה שלא נבדק בפרודקשן:** ייבוא מקובץ, התחברות ויצירת אירוע. הם דורשים חשבון
אמיתי. **זו הבדיקה שנשארה לבעל המוצר**, והיא החשובה מבין השלוש כי היא היחידה
שנוגעת בספרייה שהוחלפה.

## ✅ ו-`functions/` קיבל את התיקונים, 30/08/2026

**הסעיף הזה נכתב ב-11:51 ואמר שהפריסה טרם בוצעה. היא בוצעה ב-12:02**, אחת
עשרה דקות אחרי שנכתב.

**האימות, ולא לפי דיווח:** `deleteUserAccount`, `parseShoppingList`
ו-`onUserDeleted` מדווחות זמן עדכון בין 09:02:15 ל-09:03:22 UTC, **מול מיזוג
ב-08:45 UTC.** הפריסה מאוחרת ממנו ולכן נושאת את קובץ הנעילה החדש.

**הסעיף שנכתב אז נשמר למטה כפי שהוא.**

---

## מה שנכתב ב-11:51, לפני הפריסה

**המיזוג שחרר את צד הלקוח בלבד.** שש עשרה הפגיעויות שנסגרו ב-`functions/`
יושבות בקובץ הנעילה ולא בענן, **עד שהפקודה תרוץ:**

```powershell
$env:FUNCTIONS_DISCOVERY_TIMEOUT=60; firebase deploy --only functions --project shishi-shitufimt
```

---

## המצב הקודם, למי שקורא לאחור

### הנתונים של 30/08/2026, לפני התיקון

**האפליקציה: 18 פגיעויות ייצור**, מהן 2 קריטיות ו-5 גבוהות.
**`functions/`: 26**, מהן 3 קריטיות ו-15 גבוהות.

**ושתיים מהן היו על מסלול שהמוצר באמת מריץ:**

**`xlsx`, זיהום אב טיפוס.** המוצר קורא בעצמו קובץ שהמשתמש מעלה,
`ImportItemsModal.tsx:364`. קלט לא מהימן הגיע ישירות לספרייה הפגיעה.

**`react-router`, XSS דרך הפניה פתוחה.** ספריית הניווט של האפליקציה כולה,
ורצה אצל כל מבקר. **התיקון היה עליית גרסה בתוך גרסה ראשית 7**, ולכן לא הושקע
זמן בהוכחה שהמסלול נגיש. ראה `VERIFICATION-BUDGET.md`.

### 23/08/2026

`npm audit` ב-`functions/` הראה 26 פגיעויות, והפרונטאנד טרם נבדק אז. הריצה
נתקעה בטיימאאוט. הנתון הושלם ב-30/08.

### מה שנשאר נכון גם היום

**אין להריץ `npm audit fix --force`.** הוא מבצע שדרוגי גרסאות שוברים בלי
לשאול, ועל קוד שרץ בפרודקשן זו דרך להפוך בעיה תיאורטית לתקלה מוחשית.

**ואחרי כל שינוי ב-`functions/`, לפרוס ידנית:**

```powershell
$env:FUNCTIONS_DISCOVERY_TIMEOUT=60; firebase deploy --only functions --project shishi-shitufimt
```
