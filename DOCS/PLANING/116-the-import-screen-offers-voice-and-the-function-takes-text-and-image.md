# 116 — מסך הייבוא מציע "קול", והפונקציה בענן מקבלת טקסט ותמונה בלבד

> 🟠 **בינוני, ונפתחה 06/09/2026** בקמפיין שכתוב הניסוח של עמוד הנחיתה, אחרי
> שאותה הבטחה בדיוק תוקנה שם. **נמדד מול הפונקציה בענן ומול המסך, ולא הוערך.**
>
> ⚠️ **לא תוקן אגב.** הקמפיין נגע בעמוד הנחיתה בלבד, **וזה מסך של המארגן
> בתוך המוצר.**
>
> **ומה שהופך את זה לרשומה ולא להערה:** מי שקורא את המשפט מנסה להעביר הודעה
> קולית מוואטסאפ, **ונכשל בדיוק ברגע שהמסך הבטיח לו את הניצחון.**

## שתי מחרוזות שאומרות שאפשר להכניס קול

```
src/locales/he.json:89   importModal.smart.subtitle
"ייבא מתמונה / קול / טקסט מועתק"
```

```
src/locales/he.json:90   importModal.smart.inputLabel
"הזן רשימת פריטים (טקסט, הקלטה או תמונה)"
```

**מוצגות ב-`src/components/Admin/ImportItemsModal.tsx:936` וב-`:1027`**, והשנייה
משמשת גם כתווית נגישות ב-`:1039`.

## ומה שהפונקציה בענן מקבלת

```
functions/smartImport.js:179
const { text, image, mimeType, allowedCategories } = request.data;
```

```
functions/smartImport.js:182
if ((!text || ... ) && (!image || ... )) {
  throw new HttpsError("invalid-argument", "Please provide a shopping list text or image.");
}
```

**טקסט או תמונה. אין ערוץ שלישי.** קובץ שמע אינו נשלח, אינו מתקבל, ואין קוד
שמנסה.

## ⚠️ ומה שכן קיים, וזה מה שהופך את זה לניסוח ולא לפיצ'ר חסר

**יש הכתבה קולית חיה בדפדפן**, `src/hooks/useVoiceInput.ts`, שמשתמשת ב-
`SpeechRecognition` עם `lang` מוגדר ל-`he-IL`. **היא מוזנקת מכפתור מיקרופון
בתיבת הטקסט**, `ImportItemsModal.tsx:1062`.

**כלומר המשתמש מדבר, והדפדפן כותב טקסט לתוך התיבה.** ואז נשלח טקסט.

**זה דבר טוב, והוא עובד.** מה שאינו נכון הוא הניסוח: **"ייבא מקול" ו"הקלטה"
שניהם נקראים כמו קובץ שמע שנכנס פנימה**, ולא כמו הכתבה.

**וההבדל אינו סמנטי.** מארגן שיש בידו הודעה קולית מהקבוצה, וזה בדיוק המקרה
שהמסך הזה נבנה בשבילו, **יחפש איך להעלות אותה, ולא ימצא.**

## מה שכבר תוקן, ולמה זה מדגיש את זה

**באותו קמפיין תוקנה בדיוק אותה טענה בעמוד הנחיתה.** המחרוזת שם אמרה "הזינו
רשימה כטקסט, הודעה קולית או העלו תמונה", **והיא הוחלפה ב"הדביקו את ההודעה
מהקבוצה, הכתיבו את הרשימה בקול, או העלו צילום של פתק".**

**כלומר עמוד הנחיתה מדויק היום, והמסך שהוא מוביל אליו לא.** וזו ההצדקה
לדחיפות: **הפער נוצר בתוך המוצר עצמו, בין מה שמוכרים לבין מה שמראים.**

## ⚠️ וממצא נלווה באותו מסך

**שלוש מחרוזות עברית קשיחות בקוד, מחוץ לקובצי התרגום:**

```
src/components/Admin/ImportItemsModal.tsx:1063   aria-label: "עצור הקלטה" / "הקלטה קולית"
src/components/Admin/ImportItemsModal.tsx:1065   title:      "עצור הקלטה" / "התחל הקלטה"
```

**כלומר גם מי שגולש באנגלית שומע אותן בעברית**, וגם הן משתמשות במילה "הקלטה"
שהרשומה הזו טוענת נגדה. **שתי הבעיות באותן שתי שורות.**

## מה שמוצע, וזו אינה הכרעה

| מפתח | היום | מוצע |
|---|---|---|
| `he.json:89` | ייבא מתמונה / קול / טקסט מועתק | הדביקו טקסט, הכתיבו בקול, או העלו תמונה |
| `he.json:90` | הזן רשימת פריטים (טקסט, הקלטה או תמונה) | הזן רשימת פריטים, או הכתב אותה במיקרופון |

**והאנגלית באותה עריכה**, `src/locales/en.json`, כי שתי המחרוזות קיימות גם שם.

**ושתי המחרוזות הקשיחות עוברות לקובצי התרגום**, ומחליפות את "הקלטה" ב"הכתבה".

**מחיר: שני קובצי תרגום ושורה אחת בקוד.** לא קובץ יסוד, לא אזור ליבה.

## מה שאינו בהיקף

- **להוסיף תמיכה אמיתית בקובץ שמע.** זו יכולת, לא תיקון, **והיא גם קריאה
  בתשלום למודל**, כלומר אזור ליבה לפי CLAUDE.md.
- **עמוד הנחיתה.** תוקן בקמפיין שפתח את הרשומה הזו.
- **כל שאר המחרוזות הקשיחות במוצר.** אם יש עוד, זו רשומה משלהן.

## קשור

[57](57-central-limits-policy.md): מדיניות המגבלות על הקריאות בתשלום, שהיא
האזור שתמיכה אמיתית בשמע הייתה נוגעת בו.
