# פרומפט מבצע 06 — חוקי הפריטים והטרמפים

> נכתב על ידי צ'אט המתאם, 24/08/2026. מיועד להעתקה לצ'אט מבצע חדש.
> העתק את כל מה שמתחת לקו.

---

## 1. המשימה

בצע **שלוש תוכניות שאוחדו במודע**:

- `29-rides-blocked-by-user-items-setting` — טרמפים נחסמים בגלל הגדרה לא קשורה.
- `31-rides-consume-the-item-quota` — טרמפים אוכלים את המכסה במסך.
- `30-item-rules-trust-the-client` — המכסה ניתנת לעקיפה, והיוצר אינו מאומת.

רק אותן. שום תיקון אגבי.

**נימוק לאיחוד — והוא חריגה גדולה יותר מהרגיל, לכן היא נאמרת בגלוי.** שלושתן
אינן שלושה באגים אלא **תפר אחד**: הלקוח וחוקי המסד חלוקים על שלוש שאלות — מהי
ברירת המחדל כשההגדרה חסרה, אילו קטגוריות נחשבות טרמפ, ומי מוסמך לכתוב את מונה
הפריטים. הן יושבות באותו בלוק חוקים, באותה שכבת גישה למסד, ובאותה סוויטת בדיקות.

**ויש ביניהן תלות שאי אפשר לעקוף:** ההחלטה אם טרמפ נספר במכסה (`31`) קובעת אם
חיזוק אכיפת המונה (`30`) צריך לכלול חריג לטרמפים. לתקן את `30` לפני `31` פירושו
לקבע בחוק את ההתנהגות הלא נכונה.

**חובה לשמור אותן כשלושה קומיטים נפרדים בהיסטוריה**, בסדר שבסעיף 7.

### מחוץ להיקף — במפורש

- **אשכול המבקרים האנונימיים** — `23`, `24`, `26`. קמפיין נפרד.
- **אירוע הרפאים** (`15`), **יצירת אירועים על ידי זרים** (`20`).
- **הסיווג ל"אחר"** (`17`), **תקרת הכמות בפריט מתחלק** (`18`).
- **פירוק הקבצים הגדולים** (`13`), **קוד מת** (`06`) — גם אם תיתקל בפונקציה
  `addMenuItemAndAssign` שאין לה קורא. **מתעדים, לא מוחקים.**
- **שדרוגי תלויות** (`03`, `04`), **בדיקת התקינות השבורה** (`16`).

### שני רעיונות שנדחו — כדי שלא יחזרו

**1. "להתיר יצירת פריט בלי מונה, וזהו."** זה מה שקיים היום וזו בדיוק הפרצה:
מי שלא כותב מונה נשאר "משתתף חדש" לנצח. אומת — שמונה פריטים נוצרו ברצף באירוע
שמכסתו שלושה.

**2. "להחזיר הרשאת כתיבה רחבה כדי שהכול יעבוד."** נדחה מוחלטות, כמו בקמפיין
הקודם. ההגנות של `14` נשארות במקומן.

## 2. קרא קודם — לפני כל פעולה

בתיקיית `DOCS/PLANING/`:

- `README.md` — הלוח, האדריכלות, ומצב התשתיות.
- `29`, `31`, `30` — שלוש התוכניות, **בסדר הזה**. `29` ו-`31` מסבירות את
  ההתנהגות; `30` מסתמכת על ההכרעות שלהן.
- `28-visitor-cannot-add-own-item.md` — **רשומה סגורה, קרא בכל זאת.** היא מסבירה
  את מנגנון המונה ואת התיקון שנפרס אתמול, וכל השלוש נוגעות בו.
- `14-events-write-cascade.md` — **רשומה סגורה, קרא בכל זאת.** היא כתבה את החוק
  הזה, ו-`30` מתעד שההגנות שלה לא הושלמו.

ובקוד: `database.rules.json`, `src/services/firebaseService.ts`,
`src/pages/EventPage.tsx`, `src/components/Events/UserMenuItemForm.tsx`,
`src/components/Admin/ImportItemsModal.tsx`, ו-`test/rules/`.

## 3. הבעיה בשפה עסקית

**שלושה תסמינים, שורש אחד: המסך והשרת אינם מסכימים.**

**טרמפ נחסם בגלל הגדרה אחרת לגמרי.** בטופס האירוע יש שלוש תיבות נפרדות: הוספת
פריטים, הצעת טרמפים, בקשת טרמפים. מארגן יכול לכבות את הראשונה ולהשאיר את השנייה —
וזה אפילו לא מקרה קצה, כי **הצעת טרמפים דלוקה כברירת מחדל**. במצב הזה המשתתף רואה
את הכפתור "הצע טרמפ", ממלא את הטופס, והשרת מסרב. ההודעה שהוא מקבל אומרת
"המארגן לא איפשר הוספת פריטים באירוע זה" — נכון טכנית, מבלבל לחלוטין: הוא לא ניסה
להוסיף פריט, והמארגן דווקא כן איפשר טרמפים.

**טרמפים אוכלים את המכסה, בניגוד למה שהמוצר מבטיח.** החוקים פוטרים טרמפ מן המכסה;
שכבת הגישה למסד מעלה בכל זאת את המונה. משתתף שפרסם שלוש נסיעות באירוע שמכסתו
שלושה מגלה שאינו יכול להביא אוכל. ובנוסף, המסך מכיר בארבע קטגוריות טרמפ והחוק
מכיר בשלוש — פריט בקטגוריה הרביעית פטור לפי המסך ונספר לפי השרת.

**והמכסה עצמה אינה באמת נאכפת.** מי שמדבר עם המסד ישירות ופשוט לא כותב מונה,
יוצר פריטים בלי הגבלה. **וגם אפשר לפרסם פריט על שמו של אדם אחר** — לזה יש זנב
מכוער: הרשאות המחיקה נקבעות לפי אותו שדה, ולכן מי שהפריט נרשם על שמו יכול למחוק
אותו, ומי שיצר אותו באמת לא.

**שני האחרונים אינם קורים למשתמש רגיל** — הם דורשים לדבר עם המסד ישירות. הם
אינם חושפים מידע של אף אחד. אבל הם מבטלים הגנות שקמפיין `14` התכוון להעמיד.

## 4. שלב ביקורת — לפני שאתה נוגע במשהו

קרא את שלוש הרשומות בעיניים רעננות ואמת כל טענה מול העץ ומול האמולטור.

- לכל טענה שאימתת, הוסף `אומת [תאריך]`.
- טענה שגויה — **תקן ברשומה עצמה**, עם נימוק.

**ושלוש בדיקות שהן תנאי מקדים, ולא ניתן לדלג על אף אחת מהן:**

**א. רשימת קטגוריות הטרמפ.** מצא את שתי הרשימות — זו שבמסך וזו שבחוק — והצג
אותן זו מול זו. **וברר אם יש בפרודקשן פריטים בקטגוריה הישנה.** איחוד רשימות
שמפיל נתונים קיימים אינו איחוד.

**🛑 ב. התקרה על הטרמפים — מלכודת, לא בדיקת שגרה.**
בעל המוצר קבע שיש תקרה של **הצעה אחת והצטרפות אחת** לטרמפים. **צ'אט המתאם חיפש
אותה ולא מצא אותה** — לא בשכבת הגישה למסד, לא במסך השיבוץ ולא בבורר הקטגוריות;
כפתורי הטרמפ מוצגים לפי הגדרות האירוע בלבד. החיפוש היה ממוקד ולא ממצה.

**למה זה קריטי דווקא בקמפיין הזה:** היום מונה הפריטים הוא מה שמגביל בפועל את
כמות הטרמפים, כי הלקוח מעלה אותו גם עבורם — וזה בדיוק הבאג שאתה בא לתקן. **ברגע
שתפסיק להעלות אותו, לא יישאר שום דבר שמגביל כמה טרמפים אדם מפרסם.**

מצא את התקרה או קבע שאינה קיימת, **לפני** שאתה נוגע במונה. אם אינה קיימת — זו
רשומה חדשה, ובנייתה אינה תיקון אגבי אלא הכרעה שצריך להביא לבעל המוצר.

**🛑 ג. מסלול הייבוא של המארגן.** רשומה `30` מציעה לדרוש ששדה היוצר יהיה זהה
לכותב, **וחושדת שמסך ייבוא הפריטים שובר את זה.** אם המארגן יוצר שם פריטים
שרשומים על שם משתתפים — **הדרישה הזו תשבור את הייבוא, וזה פיצ'ר מרכזי.**
בדוק זאת לפני שאתה מציע ניסוח כלשהו לשדה היוצר.

## 5. ההכרעות כבר התקבלו — אל תשאל אותן שוב

**בעל המוצר הכריע ב-24/08/2026.** ההכרעות רשומות בגוף הרשומות עצמן, במילים
שלו. אל תפתח אותן מחדש ואל תבקש אישור חוזר — הן נתון.

**הכרעה 1 — טרמפ אפשרי גם כשהוספת פריטים כבויה.** בלשונו: *"טרמפ אפשרי כשהוספת
פריטים כבויה כי הוא ניזון מהגדרה נפרדת ב-UI."* שלוש ההגדרות בטופס האירוע נפרדות
ובלתי תלויות, והצימוד ביניהן אינו מכוון. **לנתק אותו בחוק ובבדיקה שבצד הלקוח
כאחד** — תיקון של צד אחד בלבד רק מחליף איזה צד דוחה.

**הכרעה 2 — אירועים ישנים אינם שיקול.** בלשונו: *"אירועים ישנים לא מעניינים
אותי."* כלומר אין צורך לשמר התנהגות מתירנית עבור אירוע שההגדרה חסרה בו.
**ליישר את הלקוח אל השרת: הגדרה חסרה פירושה אסור.**

**הכרעה 3 — טרמפ אינו נספר במכסת הפריטים, אבל יש לו תקרה משלו.** בלשונו:
*"טרמפ לא נספר במכסה אבל כרגע יש מגבלה של הצטרפות אחת והצעה אחת — כי לא סביר
לי שצריך יותר."* כלומר: להפסיק להעלות את מונה הפריטים בעת יצירת טרמפ, לאחד את
שתי רשימות הקטגוריות — **ולוודא שקיימת תקרה של הצעה אחת והצטרפות אחת למשתתף
באירוע.** ראה את אזהרת המלכודת בסעיף 4.

**מה שנשאר פתוח: שדה היוצר בלבד** (`30`). ההכרעה תלויה בבדיקה ב' שבסעיף 4,
ותישאל רק אחריה.
*המלצה מותנית:* אם ייבוא הפריטים אינו יוצר פריטים בשם אחרים — לדרוש התאמה בין
שדה היוצר לכותב. אם כן — לדרוש התאמה **או** שהכותב הוא מארגן האירוע. אל תמציא
ניסוח לפני שהתשובה בידך.

## 6. שלב עצירה — אישור לפני שורת קוד ראשונה

הגש תוכנית מימוש **קובץ-קובץ וקומיט-קומיט**, והמתן לאישור מפורש. בלי אישור —
לא נכתבת שורה אחת.

התוכנית חייבת להראות, לכל שינוי בחוק, **מה נהיה מותר שלא היה מותר** — ולא רק מה
נחסם. הרחבת הרשאה שנכנסת אגב תיקון היא בדיוק מה שקמפיין `14` בא למנוע.

## 7. שלב יישום

ענף חדש מעל `main` המעודכן. `git fetch` תחילה.

**קומיט 1 — לנתק את הטרמפים מהגדרת הפריטים** (`29`), בחוק ובבדיקה שבצד הלקוח
כאחד. זו התקלה החיה, ולכן ראשונה.

**קומיט 2 — להפסיק לספור טרמפים במכסה ולאחד את רשימות הקטגוריות** (`31`).

**קומיט 3 — לאכוף את המונה ואת שדה היוצר** (`30`), לפי הכרעה 3 ולפי מה שיוכרע בשדה היוצר.
**אחרון בכוונה:** הידוק לפני שהוכרע מה נספר יקבע בחוק את ההתנהגות הלא נכונה.

**כל קומיט מתחיל בבדיקה שנכשלת.** הסוויטה ב-`test/rules/` מונה 42 בדיקות והיא
הכלי המרכזי כאן — היא מבודדת נתיב מול נתיב מול האמולטור. הרץ כל בדיקה חדשה מול
החוקים הנוכחיים והצג את הפלט שבו היא נכשלת, לפני התיקון.

## 8. שלב אימות — הרצה אמיתית, לא תיאורטית

הסוויטה המלאה, ואז בדיקה חיה. עם תיעוד פלט של כל צעד:

1. אירוע שבו **הוספת פריטים כבויה והצעת טרמפים דלוקה** — משתתף מציע טרמפ
   **ומצליח**. היום נכשל.
2. באותו אירוע, משתתף מנסה להוסיף פריט רגיל — **נחסם**, וההודעה מסבירה נכון.
3. משתתף מפרסם שלוש נסיעות באירוע שמכסתו שלושה, ואז **מוסיף פריט אוכל —
   ומצליח**. היום הכפתור אפור.
4. **המכסה עדיין נאכפת** על פריטים רגילים, והפריט מעבר לה נחסם — במסך ובכתיבה
   ישירה למסד כאחת.
5. **פריט על שם אדם אחר נדחה**, לפי ההכרעה משאלה 4.
6. **ייבוא הפריטים של המארגן ממשיך לעבוד במלואו.** זו הבדיקה שמוכיחה שלא שברת
   פיצ'ר מרכזי בדרך.
7. שיבוץ לפריט קיים, עריכה ומחיקה על ידי המארגן — ממשיכים לעבוד.

### ⚠️ סדר פריסה — הלקח מהקמפיין הקודם

**מיזוג לענף הראשי הוא כשלעצמו שחרור לפרודקשן של הפרונטאנד.** Netlify פורס
אוטומטית בכל דחיפה, בלי שאיש לוחץ על כלום. החוקים, לעומת זאת, נפרסים ידנית
ב-`firebase deploy --only database --project shishi-shitufimt`.

בקמפיין הקודם זה יצר מצב שבו חצי מהתיקון היה חי וחצי לא, במשך שעות. **אל
תמזג לפני שאתה מוכן לשחרר את צד הלקוח**, ותכנן את פריסת החוקים לאותו חלון זמן.

## 9. אימות עצמי — חובה, לפני דיווח הסיום

קרא את ה-diff המלא שלך בעיניים רעננות והגש **חשבון שלמות** מול התוכנית שאושרה.
כל סעיף הוא **בוצע**, **נדחה במודע** (ורשום, עם נימוק), או **פתוח**.

בפרט הצהר בנפרד: **אילו הרשאות התרחבו בקמפיין הזה**, ולמה כל אחת מהן נדרשת.

## 10. סגירת מסמכים

עבור אחד-אחד ודווח: עודכן / לא רלוונטי / פתוח.

- `29`, `31`, `30` — לסמן מיושם, כל אחת עם ההכרעה שהתקבלה בה והנימוק.
- `14-events-write-cascade.md` — **סגורה, רק להוסיף הערה:** אילו הגנות הושלמו
  כאן. לא לפתוח מחדש.
- `18-splittable-quantity-race.md` — לבדוק אם משהו כאן שינה את התמונה שם.
- `test/rules/README.md` — לעדכן את מספר הבדיקות.
- `README.md` של תיקיית התכנון — לעדכן את הלוח.
- **למחוק את ענף הקמפיין אחרי המיזוג.** זה חלק מסגירת הקמפיין ולא משימה נפרדת —
  ראה [05-branch-cleanup.md](05-branch-cleanup.md), שם זה כבר נכשל פעמיים.

"פתוח" מקבל רשומה כתובה, לא הבטחה בעל פה.

## 11. כללים מחייבים

- **אין דחיפה ואין מיזוג בלי אישור מפורש** של המשתמש, בכל פעם מחדש. **מיזוג
  הוא פריסה** — ראה סעיף 8.
- **אין פריסת חוקים בלי אישור מפורש.**
- **באג חדש שתמצא — מתועד ברשומה חדשה, לא מתוקן אגב.**
- **אין עריכת קובצי יסוד בלי אישור נקודתי:** `README.md` בשורש, `firebase.json`,
  `.firebaserc`, `package.json`, `functions/package.json`. **`database.rules.json`
  הוא קובץ יסוד והמשימה דורשת לערוך אותו** — בקש אישור נקודתי לפני כל קומיט
  שנוגע בו.
- **אין נגיעה בפונקציות בענן.**
- **אין פירוק או ארגון מחדש של קבצים.**
- **`npm run lint` שבור** וקורס על כל קובץ (`16-lint-broken.md`). אל תריץ ואל
  תתקן.

## 12. זמני ריצה

- סוויטת חוקי המסד היא הכלי המרכזי. דורשת JDK 21, רצה מול האמולטור, ופקודת
  ההרצה המדויקת ב-`test/rules/README.md` — כולל דגל הריצה הטורית שאסור להשמיט.
- הרצה ממוקדת אחרי כל קומיט, סוויטה מלאה לפני המיזוג.
- `npm run build` לפני מיזוג — הקמפיין נוגע בפרונטאנד, וזו בדיקת התקינות היחידה
  שעובדת בפרויקט.

## 13. תקן הדיווח

- **עברית פשוטה, בלי טבלאות.**
- **ברמה של לוגיקה עסקית** — כך שאדם לא טכני יבין בקריאה ראשונה. שמות טכניים
  כפרט משני בלבד; כשפרט טכני חשוב להחלטה — קודם מה זה אומר למוצר, אחר כך השם.
- **נושאים בשורות נפרדות**, עם בולטים או מספרים, כדי שתהיה הפרדה ויזואלית ברורה
  בין נושא לנושא ובין תקלה לתקלה.
- **כל דבר בשמו במוצר** — מסך בשם המסך, מכונה בשמה המלא. לא קיצור פנימי ולא
  תיאור שהמצאת. לא בטוח בשם? אמת אותו בקוד.
- **סיום קבוע:** רשימת "קבצים שנערכו", כל קובץ כקישור לחיץ, בהפרדה בין תיעוד
  לקוד — ואחריה **משפט סיכום אחד נושא הקשר מלא**, שעומד בפני עצמו ואינו נשען
  על זיכרון השיחה. המשתמש קורא לעיתים רק את ההודעה האחרונה.
