# CLAUDE.md

**כל צ'אט בפרויקט הזה קורא את הקובץ הזה. לכן הוא קצר, וכל שורה בו נמדדה.**

## המוצר

**Shishi-Shitufi.** תיאום ארוחות קהילתיות. מארגן פותח אירוע ורשימת פריטים,
שולח קישור הזמנה בוואטסאפ, **והמוזמנים תופסים פריטים ומציעים טרמפים בלי
להירשם.**

**שני משתמשים ולא אחד: המארגן והמשתתף.** דיווח על מסך שמשתתף רואה נכתב
במונחיו.

אפליקציית דפדפן, React ו-TypeScript, על Netlify. Firebase Realtime Database,
ושלוש פונקציות בענן.

## ⚠️ פריסה: מיזוג ל-`main` הוא שחרור לפרודקשן

**Netlify בונה ומעלה אוטומטית על דחיפה. אין שלב ביניים.**

**ושני דברים אינם עולים עם המיזוג ונפרסים ידנית:**

```bash
firebase deploy --only database --project shishi-shitufimt
```

```powershell
$env:FUNCTIONS_DISCOVERY_TIMEOUT=60; firebase deploy --only functions --project shishi-shitufimt
```

⚠️ **בלי דגל ההשהיה פריסת הפונקציות נכשלת עם `Timeout after 10000`.**

**אין דחיפה, מיזוג או פריסה בלי הוראה מפורשת של בעל המוצר.**

## ⚠️ מה שבור וידוע, אל תתקן אגב

**`npm run lint` שבור.** אל תריץ ואל תתקן. רשומה 16.

**`tsc --noEmit` בלי דגל אינו בודק דבר**, כי קובץ ההגדרות הראשי ריק ומפנה
הלאה. **הפקודה שבודקת:**

```bash
npx tsc --noEmit -p tsconfig.app.json
```

**קו הבסיס: מאה וארבעים שגיאות קיימות. אסור להוסיף אחת.**

## בדיקות

```bash
node --test test/import/*.test.mjs
```

⚠️ **נתיב תיקייה נכשל, צריך את התבנית.**

**וסוויטת חוקי המסד, שדורשת JDK 21:**

```bash
firebase emulators:exec --only database --project demo-shishi "node --test --test-concurrency=1 test/rules/*.test.mjs"
```

⚠️ **`--test-concurrency=1` אינו קישוט.** כל הבדיקות חולקות אמולטור אחד.

## קובצי יסוד: אישור נקודתי לכל עריכה

`CLAUDE.md`, `database.rules.json`, `firebase.json`, `.firebaserc`,
`package.json`, `functions/package.json`, `index.html`, ו-`README.md` שבשורש.

## אזורי ליבה: שני סבבי בדיקה ואימות חי

**מקום שבו טעות בלתי הפיכה, נוגעת במידע אישי, בכסף, בהרשאות, או בהבטחה
שהמוצר נתן בכתב.**

1. **מחיקת חשבון והניקוי שאחריה.** חובה לפי תיקון 13.
2. **חוקי המסד.** הרשאות וזהות.
3. **תפוגת מידע אישי מאירועים ישנים.**
4. **הקריאות בתשלום למודל**, `functions/smartImport.js`.
5. **תנאי השימוש ומדיניות הפרטיות.**

**בכל השאר: סבב אחד של "מה פספסתי" מול העץ, ובזה מסתפקים.**

## כתיבה

**שמות במוצר נלקחים מ-`src/locales/he.json`**, ולצידו `en.json`. **אמת שם
לפני שאתה כותב אותו.**

⚠️ **מפתחות תרגום בעברית ובאנגלית בלבד.** `es.json` קיים **וספרדית מחוץ
להיקף בהכרעת בעל המוצר**, אי אפשר לבחור אותה בממשק כלל.

**בלי מקפים מפרידים**, בטקסט שהמשתמש רואה ובדיווחים. פסיק, נקודתיים, או
משפט חדש. `DOCS/PLANING/WRITING-STYLE.md`.

**לפני בדיקה שעולה זמן ריצה: האם היא נחוצה, והאם בעל המוצר יעשה אותה מהר
יותר.** `DOCS/PLANING/VERIFICATION-BUDGET.md`.

## תיעוד

**`DOCS/PLANING`.** רשומה אחת לממצא, ממוספרת. הלוח ב-`README.md` שבתוכה,
והפרומפטים ב-`prompts/`.

⚠️ **באג חדש נפתח כרשומה ולא מתוקן אגב.** גם קטן. גם כשזה מפתה.

**ורשומה היא ראיה שנכתבה על עץ אחר, לא הוראה.** אמת כל טענה נושאת משקל שלה
מול הקוד, **ודווח מה לא שרד.**

## ⚠️ אימות

**"הקוד נראה נכון" אינו אימות.**

**פריסה מאומתת מול האתר החי**, `shishi-shitufi.netlify.app`: למשוך את החבילה
שמוגשת ולחפש בה מחרוזת שהסבב הוסיף. **פונקציות מאומתות מול הענן.**

**זה תפס סבבים שדיווחו "נפרס" ולא נפרסו.**
