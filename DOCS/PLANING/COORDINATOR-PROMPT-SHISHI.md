# פרומפט המתאם של Shishi-Shitufi, גרסה 2

> מולא על ידי צ'אט המתאם 05/09/2026, **כל שדה אומת מול העץ ולא מהזיכרון.**
> **מיועד להעתקה לצ'אט מתאם חדש בפרויקט הזה.** העתק מהקו ומטה.
>
> ⚠️ **פער אחד ידוע: אין בפרויקט `CLAUDE.md`.** ראה הערה בסוף הקובץ.

---

[פרטי הפרויקט]

- **הפרויקט: Shishi-Shitufi.** אפליקציה לתיאום ארוחות קהילתיות: מארגן פותח
  אירוע ורשימת פריטים, שולח קישור הזמנה בוואטסאפ, והמוזמנים תופסים פריטים
  ומציעים טרמפים בלי להירשם. רצה כאפליקציית דפדפן על Netlify, עם Firebase
  Realtime Database ושלוש פונקציות בענן.
- **המשתמש של המוצר: המארגן.** הוא מי שפותח אירוע ומנהל אותו. **והמשתתף**
  הוא מי שנכנס מקישור, תופס פריט ומציע טרמפ, ולרוב אינו רשום כלל. **דיווח
  שנוגע במסך שמשתתף רואה נכתב במונחיו ולא במונחי המארגן.**
- **קובצי היסוד:** ⚠️ **אין `CLAUDE.md` בפרויקט הזה.** מה שממלא את מקומו,
  ודורש אישור נקודתי לכל עריכה: `database.rules.json`, `firebase.json`,
  `.firebaserc`, `package.json`, `functions/package.json`, `index.html`,
  ו-`README.md` שבשורש. **ובנוסף שני קובצי כללים שכל פרומפט מפנה אליהם:**
  `DOCS/PLANING/WRITING-STYLE.md` ו-`DOCS/PLANING/VERIFICATION-BUDGET.md`.
- **תיקיית הרשומות: `DOCS/PLANING`.** הלוח הוא `DOCS/PLANING/README.md`,
  והפרומפטים ב-`DOCS/PLANING/prompts`.
- **קובץ שמות המוצר: `src/locales/he.json`**, ולצידו `en.json`. ⚠️ **קיים גם
  `es.json` והוא מחוץ להיקף בהכרעת בעל המוצר מ-26/08/2026:** אי אפשר לבחור
  ספרדית בממשק בכלל. **מפתחות תרגום נכתבים בעברית ובאנגלית בלבד.**
- **אזורי הליבה** (רשימה סגורה, לפי הקריטריון של `core_areas`):
  1. **מחיקת חשבון והניקוי שאחריה.** בלתי הפיך, מידע אישי, וחובה לפי תיקון
     13. תוקן על פני שבע רשומות.
  2. **חוקי המסד**, `database.rules.json`. הרשאות וזהות, **ונפרסים ידנית.**
  3. **תפוגת מידע אישי מאירועים ישנים.** מחיקה שאין ממנה חזרה. רשומה 41.
  4. **הקריאות בתשלום למודל**, `functions/smartImport.js`. כסף בלי תקרה
     ידועה עד שנקבעת.
  5. **תנאי השימוש ומדיניות הפרטיות.** מה שהמוצר מבטיח, ומחויבות משפטית.
- **הענף הראשי: `main`.**
- **שפת הדיווח: עברית.**

---

## ⚠️ עובדות הפרויקט שאסור לגלות מחדש בכל סבב

**כולן נמדדו. הן קיצור דרך, לא תחליף לאימות מצב.**

**מיזוג ל-`main` הוא פריסה לפרודקשן.** Netlify בונה ומעלה אוטומטית על דחיפה.
**כלומר מיזוג הוא שחרור, ואין שלב ביניים.**

**⚠️ ושני דברים אינם עולים עם המיזוג ונפרסים ידנית:**

```powershell
firebase deploy --only database --project shishi-shitufimt
$env:FUNCTIONS_DISCOVERY_TIMEOUT=60; firebase deploy --only functions --project shishi-shitufimt
```

**בלי דגל ההשהיה, פריסת הפונקציות נכשלת עם `Timeout after 10000`.**

**`npm run lint` שבור.** רשומה 16. **אל תריץ ואל תתקן אגב.**

**בדיקת הטיפוסים היא `tsc --noEmit -p tsconfig.app.json`.** ⚠️ **בלי הדגל היא
אינה בודקת דבר**, כי קובץ ההגדרות הראשי ריק ומפנה הלאה. **קו הבסיס: מאה
וארבעים שגיאות קיימות.**

**בדיקות: `node --test test/import/*.test.mjs`.** ⚠️ **נתיב תיקייה נכשל**,
צריך את התבנית. **ויש סוויטת חוקי מסד ב-`test/rules/`** שדורשת JDK 21, ופקודת
ההרצה המדויקת ב-`test/rules/README.md` **כולל דגל ריצה טורית שאין להשמיט.**

**האתר החי: `shishi-shitufi.netlify.app`.** שאר הקישורים ב-`README.md` שבשורש
רקובים, רשומה 22.

**⚠️ ואימות פריסה נעשה מול האתר החי ולא לפי דיווח:** למשוך את החבילה
שמוגשת ולחפש בה מחרוזת שהסבב הוסיף. **זה תפס סבבים שדיווחו "נפרס" ולא נפרסו.**

---

[מכאן ואילך הפרומפט כפי שנמסר, בלי שינוי: role, measured_or_not,
report_format, decision_format, context_before_questions, autonomy_and_stops,
what_next, core_areas, test_tiers, operating_prompt_template, examples,
compaction, first_action.]

---

## ⚠️ ההערה על `CLAUDE.md`, וההכרעה שממתינה

**הפרומפט מתייחס ל-`CLAUDE.md` כחוקה. הוא אינו קיים בפרויקט הזה.** נמדד
05/09/2026, בשורש וב-`.claude/`.

**מה שמילא את מקומו עד היום: העובדות שלמעלה, שנכתבו מחדש בכל פרומפט מבצע
בנפרד.** זה עבד, **וזה גם המקום שבו נשכחים דברים.**

**ההבדל שמכריע: `CLAUDE.md` נקרא אוטומטית על ידי כל צ'אט בפרויקט.** קובץ
בתיקיית התכנון נקרא רק כשפרומפט מפנה אליו בשם. **כלומר כלל שיושב ב-`CLAUDE.md`
חל על מבצע שהמתאם שכח להזכיר לו אותו, וכלל שיושב ב-`DOCS/PLANING` לא.**

**מוצע: לכתוב `CLAUDE.md` שמכיל את קטע "עובדות הפרויקט" שלמעלה, את רשימת
קובצי היסוד, ואת שני כללי הכתיבה.** ואז השדה בפרטי הפרויקט מפנה אליו כרגיל.
