{
  "rules": {
    "users": {
      "$uid": {
        ".read": "$uid === auth.uid",
        ".write": "$uid === auth.uid",
        // How many events one organizer may create.
        //
        // The counter lives here because this is the only place that knows
        // about a person across events, and it is a validate rule rather than a
        // write rule because the owner already has a blanket write over their
        // own record. A validate rule applies whatever granted the write, so
        // this one holds even against that.
        //
        // Coming down is tied to a real deletion the same way every other
        // counter here is: the write names the event it is removing, and the
        // step is allowed only when that event was this person's and is gone
        // once the write lands. Without that the counter could simply be walked
        // back down and the ceiling would mean nothing.
        //
        // An organizer who already has events but no counter starts from one,
        // because nothing can count their existing events either. They get the
        // full ceiling on top of what they already have, which is a ceiling
        // seven events higher than intended for the busiest organizer today and
        // still a ceiling.
        // See DOCS/PLANING/57-central-limits-policy.md.
        // The organizer's own display name, which is copied onto every event
        // they create as organizerName.
        "name": { ".validate": "newData.isString() && newData.val().length <= 40" },
        "eventCount": { ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 50 && ( (!data.exists() && newData.val() === 1) || newData.val() === data.val() + 1 || (newData.val() === data.val() - 1 && root.child('events').child(newData.parent().child('eventRemoval').val()).child('organizerId').val() === auth.uid && !newData.parent().parent().parent().child('events').child(newData.parent().child('eventRemoval').val()).exists()) )" },
        "presetLists": {
          ".read": "auth != null && auth.uid == $uid",
          ".write": "auth != null && auth.uid == $uid",
          "$listId": {
            ".read": "auth != null && auth.uid == $uid",
            ".write": "auth != null && auth.uid == $uid"
          }
        }
      }
    },
    "events": {
      ".indexOn": ["organizerId"],
      // Listing is allowed only for the query "events I organised". A plain
      // read of the whole node is denied, so the event list can no longer be
      // scanned. Both spellings are accepted because the client SDK sends
      // equalTo() on the wire as a range whose bounds are equal.
      ".read": "auth != null && query.orderByChild === 'organizerId' && ( query.equalTo === auth.uid || ( query.startAt === auth.uid && query.endAt === auth.uid ) )",
      // There is deliberately no ".write" here any more. It used to be
      // "auth != null", which let anyone the app gives an identity to write
      // anywhere under events and made every rule below it a dead letter.
      // See DOCS/PLANING/14-events-write-cascade.md.
      "$eventId": {
        ".read": "auth != null",
        // How long the text in this event may be. Every one of these is a
        // validate rule rather than a write rule, and that is what makes them
        // apply to the organizer as well: a validate rule is not waived by the
        // blanket write the organizer has over their own event, so the smart
        // migration, which writes every field in the event at once, goes
        // through all of them.
        //
        // The count is in characters and not in bytes, which was an open
        // question and is now a measured one. Every Hebrew letter is two bytes,
        // so had it been bytes then every ceiling here would have been half of
        // what it says for the people who actually use this.
        //
        // Each number sits clearly above the longest value the database
        // actually held on 26/08/2026. Three of them were raised from the first
        // proposal for that reason: the numbers first suggested were below
        // values already stored, and a ceiling that cuts real data stops the
        // organizer editing an item they can see on their own screen.
        // See DOCS/PLANING/57-central-limits-policy.md.
        "organizerName": { ".validate": "newData.isString() && newData.val().length <= 40" },
        // Creating is allowed only to somebody who registers themselves as the
        // organizer; an event that already exists is judged on the organizer
        // stored in it, and on nothing else.
        //
        // The third clause this replaces asked whether the data *arriving*
        // named the writer as the organizer. That is a question the writer
        // answers for themselves: one write saying "the organizer is me" was
        // approved on the strength of what that same write had just said, and
        // the app hands an identity to every visitor who opens an invitation
        // link. The event changed hands with its details, its menu and every
        // sign-up, the real organizer was locked out of their own screens, and
        // a second write then deleted the event outright.
        //
        // The clause is folded into the first rather than deleted. Deleting it
        // closes the takeover and leaves "!data.exists()" standing on its own,
        // which is what lets anybody plant a node under events at any id with
        // any content at all.
        // See DOCS/PLANING/63-anyone-can-take-over-an-existing-event.md
        // and DOCS/PLANING/20-anyone-can-create-an-event-node.md.
        ".write": "auth != null && ( (!data.exists() && newData.child('organizerId').val() === auth.uid && newData.parent().parent().child('users').child(auth.uid).child('eventCount').val() === (root.child('users').child(auth.uid).child('eventCount').exists() ? root.child('users').child(auth.uid).child('eventCount').val() + 1 : 1)) || data.child('organizerId').val() === auth.uid )",
        "details": {
          ".write": "auth != null && ( !data.exists() || root.child('events').child($eventId).child('organizerId').val() === auth.uid )",
          // How far ahead an event may be set. An event three years out is
          // three years of everybody's names and phone numbers kept alive by
          // one date field, and until now nothing said no.
          //
          // The alternative, deleting records some months after they were
          // created, was rejected: that clock can come for an event that has
          // not happened yet. A ceiling on the date limits what goes in rather
          // than what is kept, so it can never delete anything live.
          //
          // It is a table because it has to be. A rule can read the clock as a
          // number of milliseconds and it can compare two dates written as
          // text, but it cannot turn one into the other, so there is no
          // expression for "twelve months from today". Instead there is one
          // clause per month, saying how far ahead an event may fall while that
          // month is the current one. The clauses are OR'd and the ceilings only
          // grow, so whichever month the clock is in, the most permissive clause
          // switched on is that month's own.
          //
          // Generated for twenty years from August 2026. It rolls by itself
          // until August 2046 and then stops rolling, so it wants regenerating
          // well before that.
          //
          // The end date carries the same ceiling, and that is the half that is
          // easy to miss: limiting only the start leaves an event that begins
          // tomorrow and ends in the year 3000. The end also has to come after
          // the start, a rule that until now lived in the form and therefore did
          // not exist for anybody writing to the database directly.
          // See DOCS/PLANING/65-end-date-is-written-and-never-read.md
          // and DOCS/PLANING/57-central-limits-policy.md.
          "title": { ".validate": "newData.isString() && newData.val().length <= 50" },
          "location": { ".validate": "newData.isString() && newData.val().length <= 100" },
          "description": { ".validate": "newData.isString() && newData.val().length <= 500" },
          // The category list is stored as an array, so the wildcard here is a
          // position in it and not an id. That shape is also why the ride
          // exemption could not be tied to it: a rule cannot search a list.
          "categories": { "$categoryIndex": { "name": { ".validate": "newData.isString() && newData.val().length <= 30" } } },
          "date": { ".validate": "newData.isString() && newData.val().matches(/^[0-9]{4}-[0-9]{2}-[0-9]{2}$/) && ((now >= 0 && newData.val() <= '2027-08-31') || (now >= 1788220800000 && newData.val() <= '2027-09-30') || (now >= 1790812800000 && newData.val() <= '2027-10-31') || (now >= 1793491200000 && newData.val() <= '2027-11-30') || (now >= 1796083200000 && newData.val() <= '2027-12-31') || (now >= 1798761600000 && newData.val() <= '2028-01-31') || (now >= 1801440000000 && newData.val() <= '2028-02-29') || (now >= 1803859200000 && newData.val() <= '2028-03-31') || (now >= 1806537600000 && newData.val() <= '2028-04-30') || (now >= 1809129600000 && newData.val() <= '2028-05-31') || (now >= 1811808000000 && newData.val() <= '2028-06-30') || (now >= 1814400000000 && newData.val() <= '2028-07-31') || (now >= 1817078400000 && newData.val() <= '2028-08-31') || (now >= 1819756800000 && newData.val() <= '2028-09-30') || (now >= 1822348800000 && newData.val() <= '2028-10-31') || (now >= 1825027200000 && newData.val() <= '2028-11-30') || (now >= 1827619200000 && newData.val() <= '2028-12-31') || (now >= 1830297600000 && newData.val() <= '2029-01-31') || (now >= 1832976000000 && newData.val() <= '2029-02-28') || (now >= 1835481600000 && newData.val() <= '2029-03-31') || (now >= 1838160000000 && newData.val() <= '2029-04-30') || (now >= 1840752000000 && newData.val() <= '2029-05-31') || (now >= 1843430400000 && newData.val() <= '2029-06-30') || (now >= 1846022400000 && newData.val() <= '2029-07-31') || (now >= 1848700800000 && newData.val() <= '2029-08-31') || (now >= 1851379200000 && newData.val() <= '2029-09-30') || (now >= 1853971200000 && newData.val() <= '2029-10-31') || (now >= 1856649600000 && newData.val() <= '2029-11-30') || (now >= 1859241600000 && newData.val() <= '2029-12-31') || (now >= 1861920000000 && newData.val() <= '2030-01-31') || (now >= 1864598400000 && newData.val() <= '2030-02-28') || (now >= 1867017600000 && newData.val() <= '2030-03-31') || (now >= 1869696000000 && newData.val() <= '2030-04-30') || (now >= 1872288000000 && newData.val() <= '2030-05-31') || (now >= 1874966400000 && newData.val() <= '2030-06-30') || (now >= 1877558400000 && newData.val() <= '2030-07-31') || (now >= 1880236800000 && newData.val() <= '2030-08-31') || (now >= 1882915200000 && newData.val() <= '2030-09-30') || (now >= 1885507200000 && newData.val() <= '2030-10-31') || (now >= 1888185600000 && newData.val() <= '2030-11-30') || (now >= 1890777600000 && newData.val() <= '2030-12-31') || (now >= 1893456000000 && newData.val() <= '2031-01-31') || (now >= 1896134400000 && newData.val() <= '2031-02-28') || (now >= 1898553600000 && newData.val() <= '2031-03-31') || (now >= 1901232000000 && newData.val() <= '2031-04-30') || (now >= 1903824000000 && newData.val() <= '2031-05-31') || (now >= 1906502400000 && newData.val() <= '2031-06-30') || (now >= 1909094400000 && newData.val() <= '2031-07-31') || (now >= 1911772800000 && newData.val() <= '2031-08-31') || (now >= 1914451200000 && newData.val() <= '2031-09-30') || (now >= 1917043200000 && newData.val() <= '2031-10-31') || (now >= 1919721600000 && newData.val() <= '2031-11-30') || (now >= 1922313600000 && newData.val() <= '2031-12-31') || (now >= 1924992000000 && newData.val() <= '2032-01-31') || (now >= 1927670400000 && newData.val() <= '2032-02-29') || (now >= 1930089600000 && newData.val() <= '2032-03-31') || (now >= 1932768000000 && newData.val() <= '2032-04-30') || (now >= 1935360000000 && newData.val() <= '2032-05-31') || (now >= 1938038400000 && newData.val() <= '2032-06-30') || (now >= 1940630400000 && newData.val() <= '2032-07-31') || (now >= 1943308800000 && newData.val() <= '2032-08-31') || (now >= 1945987200000 && newData.val() <= '2032-09-30') || (now >= 1948579200000 && newData.val() <= '2032-10-31') || (now >= 1951257600000 && newData.val() <= '2032-11-30') || (now >= 1953849600000 && newData.val() <= '2032-12-31') || (now >= 1956528000000 && newData.val() <= '2033-01-31') || (now >= 1959206400000 && newData.val() <= '2033-02-28') || (now >= 1961712000000 && newData.val() <= '2033-03-31') || (now >= 1964390400000 && newData.val() <= '2033-04-30') || (now >= 1966982400000 && newData.val() <= '2033-05-31') || (now >= 1969660800000 && newData.val() <= '2033-06-30') || (now >= 1972252800000 && newData.val() <= '2033-07-31') || (now >= 1974931200000 && newData.val() <= '2033-08-31') || (now >= 1977609600000 && newData.val() <= '2033-09-30') || (now >= 1980201600000 && newData.val() <= '2033-10-31') || (now >= 1982880000000 && newData.val() <= '2033-11-30') || (now >= 1985472000000 && newData.val() <= '2033-12-31') || (now >= 1988150400000 && newData.val() <= '2034-01-31') || (now >= 1990828800000 && newData.val() <= '2034-02-28') || (now >= 1993248000000 && newData.val() <= '2034-03-31') || (now >= 1995926400000 && newData.val() <= '2034-04-30') || (now >= 1998518400000 && newData.val() <= '2034-05-31') || (now >= 2001196800000 && newData.val() <= '2034-06-30') || (now >= 2003788800000 && newData.val() <= '2034-07-31') || (now >= 2006467200000 && newData.val() <= '2034-08-31') || (now >= 2009145600000 && newData.val() <= '2034-09-30') || (now >= 2011737600000 && newData.val() <= '2034-10-31') || (now >= 2014416000000 && newData.val() <= '2034-11-30') || (now >= 2017008000000 && newData.val() <= '2034-12-31') || (now >= 2019686400000 && newData.val() <= '2035-01-31') || (now >= 2022364800000 && newData.val() <= '2035-02-28') || (now >= 2024784000000 && newData.val() <= '2035-03-31') || (now >= 2027462400000 && newData.val() <= '2035-04-30') || (now >= 2030054400000 && newData.val() <= '2035-05-31') || (now >= 2032732800000 && newData.val() <= '2035-06-30') || (now >= 2035324800000 && newData.val() <= '2035-07-31') || (now >= 2038003200000 && newData.val() <= '2035-08-31') || (now >= 2040681600000 && newData.val() <= '2035-09-30') || (now >= 2043273600000 && newData.val() <= '2035-10-31') || (now >= 2045952000000 && newData.val() <= '2035-11-30') || (now >= 2048544000000 && newData.val() <= '2035-12-31') || (now >= 2051222400000 && newData.val() <= '2036-01-31') || (now >= 2053900800000 && newData.val() <= '2036-02-29') || (now >= 2056320000000 && newData.val() <= '2036-03-31') || (now >= 2058998400000 && newData.val() <= '2036-04-30') || (now >= 2061590400000 && newData.val() <= '2036-05-31') || (now >= 2064268800000 && newData.val() <= '2036-06-30') || (now >= 2066860800000 && newData.val() <= '2036-07-31') || (now >= 2069539200000 && newData.val() <= '2036-08-31') || (now >= 2072217600000 && newData.val() <= '2036-09-30') || (now >= 2074809600000 && newData.val() <= '2036-10-31') || (now >= 2077488000000 && newData.val() <= '2036-11-30') || (now >= 2080080000000 && newData.val() <= '2036-12-31') || (now >= 2082758400000 && newData.val() <= '2037-01-31') || (now >= 2085436800000 && newData.val() <= '2037-02-28') || (now >= 2087942400000 && newData.val() <= '2037-03-31') || (now >= 2090620800000 && newData.val() <= '2037-04-30') || (now >= 2093212800000 && newData.val() <= '2037-05-31') || (now >= 2095891200000 && newData.val() <= '2037-06-30') || (now >= 2098483200000 && newData.val() <= '2037-07-31') || (now >= 2101161600000 && newData.val() <= '2037-08-31') || (now >= 2103840000000 && newData.val() <= '2037-09-30') || (now >= 2106432000000 && newData.val() <= '2037-10-31') || (now >= 2109110400000 && newData.val() <= '2037-11-30') || (now >= 2111702400000 && newData.val() <= '2037-12-31') || (now >= 2114380800000 && newData.val() <= '2038-01-31') || (now >= 2117059200000 && newData.val() <= '2038-02-28') || (now >= 2119478400000 && newData.val() <= '2038-03-31') || (now >= 2122156800000 && newData.val() <= '2038-04-30') || (now >= 2124748800000 && newData.val() <= '2038-05-31') || (now >= 2127427200000 && newData.val() <= '2038-06-30') || (now >= 2130019200000 && newData.val() <= '2038-07-31') || (now >= 2132697600000 && newData.val() <= '2038-08-31') || (now >= 2135376000000 && newData.val() <= '2038-09-30') || (now >= 2137968000000 && newData.val() <= '2038-10-31') || (now >= 2140646400000 && newData.val() <= '2038-11-30') || (now >= 2143238400000 && newData.val() <= '2038-12-31') || (now >= 2145916800000 && newData.val() <= '2039-01-31') || (now >= 2148595200000 && newData.val() <= '2039-02-28') || (now >= 2151014400000 && newData.val() <= '2039-03-31') || (now >= 2153692800000 && newData.val() <= '2039-04-30') || (now >= 2156284800000 && newData.val() <= '2039-05-31') || (now >= 2158963200000 && newData.val() <= '2039-06-30') || (now >= 2161555200000 && newData.val() <= '2039-07-31') || (now >= 2164233600000 && newData.val() <= '2039-08-31') || (now >= 2166912000000 && newData.val() <= '2039-09-30') || (now >= 2169504000000 && newData.val() <= '2039-10-31') || (now >= 2172182400000 && newData.val() <= '2039-11-30') || (now >= 2174774400000 && newData.val() <= '2039-12-31') || (now >= 2177452800000 && newData.val() <= '2040-01-31') || (now >= 2180131200000 && newData.val() <= '2040-02-29') || (now >= 2182550400000 && newData.val() <= '2040-03-31') || (now >= 2185228800000 && newData.val() <= '2040-04-30') || (now >= 2187820800000 && newData.val() <= '2040-05-31') || (now >= 2190499200000 && newData.val() <= '2040-06-30') || (now >= 2193091200000 && newData.val() <= '2040-07-31') || (now >= 2195769600000 && newData.val() <= '2040-08-31') || (now >= 2198448000000 && newData.val() <= '2040-09-30') || (now >= 2201040000000 && newData.val() <= '2040-10-31') || (now >= 2203718400000 && newData.val() <= '2040-11-30') || (now >= 2206310400000 && newData.val() <= '2040-12-31') || (now >= 2208988800000 && newData.val() <= '2041-01-31') || (now >= 2211667200000 && newData.val() <= '2041-02-28') || (now >= 2214172800000 && newData.val() <= '2041-03-31') || (now >= 2216851200000 && newData.val() <= '2041-04-30') || (now >= 2219443200000 && newData.val() <= '2041-05-31') || (now >= 2222121600000 && newData.val() <= '2041-06-30') || (now >= 2224713600000 && newData.val() <= '2041-07-31') || (now >= 2227392000000 && newData.val() <= '2041-08-31') || (now >= 2230070400000 && newData.val() <= '2041-09-30') || (now >= 2232662400000 && newData.val() <= '2041-10-31') || (now >= 2235340800000 && newData.val() <= '2041-11-30') || (now >= 2237932800000 && newData.val() <= '2041-12-31') || (now >= 2240611200000 && newData.val() <= '2042-01-31') || (now >= 2243289600000 && newData.val() <= '2042-02-28') || (now >= 2245708800000 && newData.val() <= '2042-03-31') || (now >= 2248387200000 && newData.val() <= '2042-04-30') || (now >= 2250979200000 && newData.val() <= '2042-05-31') || (now >= 2253657600000 && newData.val() <= '2042-06-30') || (now >= 2256249600000 && newData.val() <= '2042-07-31') || (now >= 2258928000000 && newData.val() <= '2042-08-31') || (now >= 2261606400000 && newData.val() <= '2042-09-30') || (now >= 2264198400000 && newData.val() <= '2042-10-31') || (now >= 2266876800000 && newData.val() <= '2042-11-30') || (now >= 2269468800000 && newData.val() <= '2042-12-31') || (now >= 2272147200000 && newData.val() <= '2043-01-31') || (now >= 2274825600000 && newData.val() <= '2043-02-28') || (now >= 2277244800000 && newData.val() <= '2043-03-31') || (now >= 2279923200000 && newData.val() <= '2043-04-30') || (now >= 2282515200000 && newData.val() <= '2043-05-31') || (now >= 2285193600000 && newData.val() <= '2043-06-30') || (now >= 2287785600000 && newData.val() <= '2043-07-31') || (now >= 2290464000000 && newData.val() <= '2043-08-31') || (now >= 2293142400000 && newData.val() <= '2043-09-30') || (now >= 2295734400000 && newData.val() <= '2043-10-31') || (now >= 2298412800000 && newData.val() <= '2043-11-30') || (now >= 2301004800000 && newData.val() <= '2043-12-31') || (now >= 2303683200000 && newData.val() <= '2044-01-31') || (now >= 2306361600000 && newData.val() <= '2044-02-29') || (now >= 2308780800000 && newData.val() <= '2044-03-31') || (now >= 2311459200000 && newData.val() <= '2044-04-30') || (now >= 2314051200000 && newData.val() <= '2044-05-31') || (now >= 2316729600000 && newData.val() <= '2044-06-30') || (now >= 2319321600000 && newData.val() <= '2044-07-31') || (now >= 2322000000000 && newData.val() <= '2044-08-31') || (now >= 2324678400000 && newData.val() <= '2044-09-30') || (now >= 2327270400000 && newData.val() <= '2044-10-31') || (now >= 2329948800000 && newData.val() <= '2044-11-30') || (now >= 2332540800000 && newData.val() <= '2044-12-31') || (now >= 2335219200000 && newData.val() <= '2045-01-31') || (now >= 2337897600000 && newData.val() <= '2045-02-28') || (now >= 2340403200000 && newData.val() <= '2045-03-31') || (now >= 2343081600000 && newData.val() <= '2045-04-30') || (now >= 2345673600000 && newData.val() <= '2045-05-31') || (now >= 2348352000000 && newData.val() <= '2045-06-30') || (now >= 2350944000000 && newData.val() <= '2045-07-31') || (now >= 2353622400000 && newData.val() <= '2045-08-31') || (now >= 2356300800000 && newData.val() <= '2045-09-30') || (now >= 2358892800000 && newData.val() <= '2045-10-31') || (now >= 2361571200000 && newData.val() <= '2045-11-30') || (now >= 2364163200000 && newData.val() <= '2045-12-31') || (now >= 2366841600000 && newData.val() <= '2046-01-31') || (now >= 2369520000000 && newData.val() <= '2046-02-28') || (now >= 2371939200000 && newData.val() <= '2046-03-31') || (now >= 2374617600000 && newData.val() <= '2046-04-30') || (now >= 2377209600000 && newData.val() <= '2046-05-31') || (now >= 2379888000000 && newData.val() <= '2046-06-30') || (now >= 2382480000000 && newData.val() <= '2046-07-31') || (now >= 2385158400000 && newData.val() <= '2046-08-31') || (now >= 2387836800000 && newData.val() <= '2046-09-30') || (now >= 2390428800000 && newData.val() <= '2046-10-31') || (now >= 2393107200000 && newData.val() <= '2046-11-30') || (now >= 2395699200000 && newData.val() <= '2046-12-31') || (now >= 2398377600000 && newData.val() <= '2047-01-31') || (now >= 2401056000000 && newData.val() <= '2047-02-28') || (now >= 2403475200000 && newData.val() <= '2047-03-31') || (now >= 2406153600000 && newData.val() <= '2047-04-30') || (now >= 2408745600000 && newData.val() <= '2047-05-31') || (now >= 2411424000000 && newData.val() <= '2047-06-30') || (now >= 2414016000000 && newData.val() <= '2047-07-31'))" },
          "endDate": { ".validate": "newData.isString() && newData.val().matches(/^[0-9]{4}-[0-9]{2}-[0-9]{2}$/) && ((now >= 0 && newData.val() <= '2027-08-31') || (now >= 1788220800000 && newData.val() <= '2027-09-30') || (now >= 1790812800000 && newData.val() <= '2027-10-31') || (now >= 1793491200000 && newData.val() <= '2027-11-30') || (now >= 1796083200000 && newData.val() <= '2027-12-31') || (now >= 1798761600000 && newData.val() <= '2028-01-31') || (now >= 1801440000000 && newData.val() <= '2028-02-29') || (now >= 1803859200000 && newData.val() <= '2028-03-31') || (now >= 1806537600000 && newData.val() <= '2028-04-30') || (now >= 1809129600000 && newData.val() <= '2028-05-31') || (now >= 1811808000000 && newData.val() <= '2028-06-30') || (now >= 1814400000000 && newData.val() <= '2028-07-31') || (now >= 1817078400000 && newData.val() <= '2028-08-31') || (now >= 1819756800000 && newData.val() <= '2028-09-30') || (now >= 1822348800000 && newData.val() <= '2028-10-31') || (now >= 1825027200000 && newData.val() <= '2028-11-30') || (now >= 1827619200000 && newData.val() <= '2028-12-31') || (now >= 1830297600000 && newData.val() <= '2029-01-31') || (now >= 1832976000000 && newData.val() <= '2029-02-28') || (now >= 1835481600000 && newData.val() <= '2029-03-31') || (now >= 1838160000000 && newData.val() <= '2029-04-30') || (now >= 1840752000000 && newData.val() <= '2029-05-31') || (now >= 1843430400000 && newData.val() <= '2029-06-30') || (now >= 1846022400000 && newData.val() <= '2029-07-31') || (now >= 1848700800000 && newData.val() <= '2029-08-31') || (now >= 1851379200000 && newData.val() <= '2029-09-30') || (now >= 1853971200000 && newData.val() <= '2029-10-31') || (now >= 1856649600000 && newData.val() <= '2029-11-30') || (now >= 1859241600000 && newData.val() <= '2029-12-31') || (now >= 1861920000000 && newData.val() <= '2030-01-31') || (now >= 1864598400000 && newData.val() <= '2030-02-28') || (now >= 1867017600000 && newData.val() <= '2030-03-31') || (now >= 1869696000000 && newData.val() <= '2030-04-30') || (now >= 1872288000000 && newData.val() <= '2030-05-31') || (now >= 1874966400000 && newData.val() <= '2030-06-30') || (now >= 1877558400000 && newData.val() <= '2030-07-31') || (now >= 1880236800000 && newData.val() <= '2030-08-31') || (now >= 1882915200000 && newData.val() <= '2030-09-30') || (now >= 1885507200000 && newData.val() <= '2030-10-31') || (now >= 1888185600000 && newData.val() <= '2030-11-30') || (now >= 1890777600000 && newData.val() <= '2030-12-31') || (now >= 1893456000000 && newData.val() <= '2031-01-31') || (now >= 1896134400000 && newData.val() <= '2031-02-28') || (now >= 1898553600000 && newData.val() <= '2031-03-31') || (now >= 1901232000000 && newData.val() <= '2031-04-30') || (now >= 1903824000000 && newData.val() <= '2031-05-31') || (now >= 1906502400000 && newData.val() <= '2031-06-30') || (now >= 1909094400000 && newData.val() <= '2031-07-31') || (now >= 1911772800000 && newData.val() <= '2031-08-31') || (now >= 1914451200000 && newData.val() <= '2031-09-30') || (now >= 1917043200000 && newData.val() <= '2031-10-31') || (now >= 1919721600000 && newData.val() <= '2031-11-30') || (now >= 1922313600000 && newData.val() <= '2031-12-31') || (now >= 1924992000000 && newData.val() <= '2032-01-31') || (now >= 1927670400000 && newData.val() <= '2032-02-29') || (now >= 1930089600000 && newData.val() <= '2032-03-31') || (now >= 1932768000000 && newData.val() <= '2032-04-30') || (now >= 1935360000000 && newData.val() <= '2032-05-31') || (now >= 1938038400000 && newData.val() <= '2032-06-30') || (now >= 1940630400000 && newData.val() <= '2032-07-31') || (now >= 1943308800000 && newData.val() <= '2032-08-31') || (now >= 1945987200000 && newData.val() <= '2032-09-30') || (now >= 1948579200000 && newData.val() <= '2032-10-31') || (now >= 1951257600000 && newData.val() <= '2032-11-30') || (now >= 1953849600000 && newData.val() <= '2032-12-31') || (now >= 1956528000000 && newData.val() <= '2033-01-31') || (now >= 1959206400000 && newData.val() <= '2033-02-28') || (now >= 1961712000000 && newData.val() <= '2033-03-31') || (now >= 1964390400000 && newData.val() <= '2033-04-30') || (now >= 1966982400000 && newData.val() <= '2033-05-31') || (now >= 1969660800000 && newData.val() <= '2033-06-30') || (now >= 1972252800000 && newData.val() <= '2033-07-31') || (now >= 1974931200000 && newData.val() <= '2033-08-31') || (now >= 1977609600000 && newData.val() <= '2033-09-30') || (now >= 1980201600000 && newData.val() <= '2033-10-31') || (now >= 1982880000000 && newData.val() <= '2033-11-30') || (now >= 1985472000000 && newData.val() <= '2033-12-31') || (now >= 1988150400000 && newData.val() <= '2034-01-31') || (now >= 1990828800000 && newData.val() <= '2034-02-28') || (now >= 1993248000000 && newData.val() <= '2034-03-31') || (now >= 1995926400000 && newData.val() <= '2034-04-30') || (now >= 1998518400000 && newData.val() <= '2034-05-31') || (now >= 2001196800000 && newData.val() <= '2034-06-30') || (now >= 2003788800000 && newData.val() <= '2034-07-31') || (now >= 2006467200000 && newData.val() <= '2034-08-31') || (now >= 2009145600000 && newData.val() <= '2034-09-30') || (now >= 2011737600000 && newData.val() <= '2034-10-31') || (now >= 2014416000000 && newData.val() <= '2034-11-30') || (now >= 2017008000000 && newData.val() <= '2034-12-31') || (now >= 2019686400000 && newData.val() <= '2035-01-31') || (now >= 2022364800000 && newData.val() <= '2035-02-28') || (now >= 2024784000000 && newData.val() <= '2035-03-31') || (now >= 2027462400000 && newData.val() <= '2035-04-30') || (now >= 2030054400000 && newData.val() <= '2035-05-31') || (now >= 2032732800000 && newData.val() <= '2035-06-30') || (now >= 2035324800000 && newData.val() <= '2035-07-31') || (now >= 2038003200000 && newData.val() <= '2035-08-31') || (now >= 2040681600000 && newData.val() <= '2035-09-30') || (now >= 2043273600000 && newData.val() <= '2035-10-31') || (now >= 2045952000000 && newData.val() <= '2035-11-30') || (now >= 2048544000000 && newData.val() <= '2035-12-31') || (now >= 2051222400000 && newData.val() <= '2036-01-31') || (now >= 2053900800000 && newData.val() <= '2036-02-29') || (now >= 2056320000000 && newData.val() <= '2036-03-31') || (now >= 2058998400000 && newData.val() <= '2036-04-30') || (now >= 2061590400000 && newData.val() <= '2036-05-31') || (now >= 2064268800000 && newData.val() <= '2036-06-30') || (now >= 2066860800000 && newData.val() <= '2036-07-31') || (now >= 2069539200000 && newData.val() <= '2036-08-31') || (now >= 2072217600000 && newData.val() <= '2036-09-30') || (now >= 2074809600000 && newData.val() <= '2036-10-31') || (now >= 2077488000000 && newData.val() <= '2036-11-30') || (now >= 2080080000000 && newData.val() <= '2036-12-31') || (now >= 2082758400000 && newData.val() <= '2037-01-31') || (now >= 2085436800000 && newData.val() <= '2037-02-28') || (now >= 2087942400000 && newData.val() <= '2037-03-31') || (now >= 2090620800000 && newData.val() <= '2037-04-30') || (now >= 2093212800000 && newData.val() <= '2037-05-31') || (now >= 2095891200000 && newData.val() <= '2037-06-30') || (now >= 2098483200000 && newData.val() <= '2037-07-31') || (now >= 2101161600000 && newData.val() <= '2037-08-31') || (now >= 2103840000000 && newData.val() <= '2037-09-30') || (now >= 2106432000000 && newData.val() <= '2037-10-31') || (now >= 2109110400000 && newData.val() <= '2037-11-30') || (now >= 2111702400000 && newData.val() <= '2037-12-31') || (now >= 2114380800000 && newData.val() <= '2038-01-31') || (now >= 2117059200000 && newData.val() <= '2038-02-28') || (now >= 2119478400000 && newData.val() <= '2038-03-31') || (now >= 2122156800000 && newData.val() <= '2038-04-30') || (now >= 2124748800000 && newData.val() <= '2038-05-31') || (now >= 2127427200000 && newData.val() <= '2038-06-30') || (now >= 2130019200000 && newData.val() <= '2038-07-31') || (now >= 2132697600000 && newData.val() <= '2038-08-31') || (now >= 2135376000000 && newData.val() <= '2038-09-30') || (now >= 2137968000000 && newData.val() <= '2038-10-31') || (now >= 2140646400000 && newData.val() <= '2038-11-30') || (now >= 2143238400000 && newData.val() <= '2038-12-31') || (now >= 2145916800000 && newData.val() <= '2039-01-31') || (now >= 2148595200000 && newData.val() <= '2039-02-28') || (now >= 2151014400000 && newData.val() <= '2039-03-31') || (now >= 2153692800000 && newData.val() <= '2039-04-30') || (now >= 2156284800000 && newData.val() <= '2039-05-31') || (now >= 2158963200000 && newData.val() <= '2039-06-30') || (now >= 2161555200000 && newData.val() <= '2039-07-31') || (now >= 2164233600000 && newData.val() <= '2039-08-31') || (now >= 2166912000000 && newData.val() <= '2039-09-30') || (now >= 2169504000000 && newData.val() <= '2039-10-31') || (now >= 2172182400000 && newData.val() <= '2039-11-30') || (now >= 2174774400000 && newData.val() <= '2039-12-31') || (now >= 2177452800000 && newData.val() <= '2040-01-31') || (now >= 2180131200000 && newData.val() <= '2040-02-29') || (now >= 2182550400000 && newData.val() <= '2040-03-31') || (now >= 2185228800000 && newData.val() <= '2040-04-30') || (now >= 2187820800000 && newData.val() <= '2040-05-31') || (now >= 2190499200000 && newData.val() <= '2040-06-30') || (now >= 2193091200000 && newData.val() <= '2040-07-31') || (now >= 2195769600000 && newData.val() <= '2040-08-31') || (now >= 2198448000000 && newData.val() <= '2040-09-30') || (now >= 2201040000000 && newData.val() <= '2040-10-31') || (now >= 2203718400000 && newData.val() <= '2040-11-30') || (now >= 2206310400000 && newData.val() <= '2040-12-31') || (now >= 2208988800000 && newData.val() <= '2041-01-31') || (now >= 2211667200000 && newData.val() <= '2041-02-28') || (now >= 2214172800000 && newData.val() <= '2041-03-31') || (now >= 2216851200000 && newData.val() <= '2041-04-30') || (now >= 2219443200000 && newData.val() <= '2041-05-31') || (now >= 2222121600000 && newData.val() <= '2041-06-30') || (now >= 2224713600000 && newData.val() <= '2041-07-31') || (now >= 2227392000000 && newData.val() <= '2041-08-31') || (now >= 2230070400000 && newData.val() <= '2041-09-30') || (now >= 2232662400000 && newData.val() <= '2041-10-31') || (now >= 2235340800000 && newData.val() <= '2041-11-30') || (now >= 2237932800000 && newData.val() <= '2041-12-31') || (now >= 2240611200000 && newData.val() <= '2042-01-31') || (now >= 2243289600000 && newData.val() <= '2042-02-28') || (now >= 2245708800000 && newData.val() <= '2042-03-31') || (now >= 2248387200000 && newData.val() <= '2042-04-30') || (now >= 2250979200000 && newData.val() <= '2042-05-31') || (now >= 2253657600000 && newData.val() <= '2042-06-30') || (now >= 2256249600000 && newData.val() <= '2042-07-31') || (now >= 2258928000000 && newData.val() <= '2042-08-31') || (now >= 2261606400000 && newData.val() <= '2042-09-30') || (now >= 2264198400000 && newData.val() <= '2042-10-31') || (now >= 2266876800000 && newData.val() <= '2042-11-30') || (now >= 2269468800000 && newData.val() <= '2042-12-31') || (now >= 2272147200000 && newData.val() <= '2043-01-31') || (now >= 2274825600000 && newData.val() <= '2043-02-28') || (now >= 2277244800000 && newData.val() <= '2043-03-31') || (now >= 2279923200000 && newData.val() <= '2043-04-30') || (now >= 2282515200000 && newData.val() <= '2043-05-31') || (now >= 2285193600000 && newData.val() <= '2043-06-30') || (now >= 2287785600000 && newData.val() <= '2043-07-31') || (now >= 2290464000000 && newData.val() <= '2043-08-31') || (now >= 2293142400000 && newData.val() <= '2043-09-30') || (now >= 2295734400000 && newData.val() <= '2043-10-31') || (now >= 2298412800000 && newData.val() <= '2043-11-30') || (now >= 2301004800000 && newData.val() <= '2043-12-31') || (now >= 2303683200000 && newData.val() <= '2044-01-31') || (now >= 2306361600000 && newData.val() <= '2044-02-29') || (now >= 2308780800000 && newData.val() <= '2044-03-31') || (now >= 2311459200000 && newData.val() <= '2044-04-30') || (now >= 2314051200000 && newData.val() <= '2044-05-31') || (now >= 2316729600000 && newData.val() <= '2044-06-30') || (now >= 2319321600000 && newData.val() <= '2044-07-31') || (now >= 2322000000000 && newData.val() <= '2044-08-31') || (now >= 2324678400000 && newData.val() <= '2044-09-30') || (now >= 2327270400000 && newData.val() <= '2044-10-31') || (now >= 2329948800000 && newData.val() <= '2044-11-30') || (now >= 2332540800000 && newData.val() <= '2044-12-31') || (now >= 2335219200000 && newData.val() <= '2045-01-31') || (now >= 2337897600000 && newData.val() <= '2045-02-28') || (now >= 2340403200000 && newData.val() <= '2045-03-31') || (now >= 2343081600000 && newData.val() <= '2045-04-30') || (now >= 2345673600000 && newData.val() <= '2045-05-31') || (now >= 2348352000000 && newData.val() <= '2045-06-30') || (now >= 2350944000000 && newData.val() <= '2045-07-31') || (now >= 2353622400000 && newData.val() <= '2045-08-31') || (now >= 2356300800000 && newData.val() <= '2045-09-30') || (now >= 2358892800000 && newData.val() <= '2045-10-31') || (now >= 2361571200000 && newData.val() <= '2045-11-30') || (now >= 2364163200000 && newData.val() <= '2045-12-31') || (now >= 2366841600000 && newData.val() <= '2046-01-31') || (now >= 2369520000000 && newData.val() <= '2046-02-28') || (now >= 2371939200000 && newData.val() <= '2046-03-31') || (now >= 2374617600000 && newData.val() <= '2046-04-30') || (now >= 2377209600000 && newData.val() <= '2046-05-31') || (now >= 2379888000000 && newData.val() <= '2046-06-30') || (now >= 2382480000000 && newData.val() <= '2046-07-31') || (now >= 2385158400000 && newData.val() <= '2046-08-31') || (now >= 2387836800000 && newData.val() <= '2046-09-30') || (now >= 2390428800000 && newData.val() <= '2046-10-31') || (now >= 2393107200000 && newData.val() <= '2046-11-30') || (now >= 2395699200000 && newData.val() <= '2046-12-31') || (now >= 2398377600000 && newData.val() <= '2047-01-31') || (now >= 2401056000000 && newData.val() <= '2047-02-28') || (now >= 2403475200000 && newData.val() <= '2047-03-31') || (now >= 2406153600000 && newData.val() <= '2047-04-30') || (now >= 2408745600000 && newData.val() <= '2047-05-31') || (now >= 2411424000000 && newData.val() <= '2047-06-30') || (now >= 2414016000000 && newData.val() <= '2047-07-31')) && (!newData.parent().child('date').exists() || newData.val() >= newData.parent().child('date').val())" },
          "endTime": { ".validate": "newData.isString() && ( !newData.parent().child('endDate').exists() || !newData.parent().child('date').exists() || !newData.parent().child('time').exists() || newData.parent().child('endDate').val() !== newData.parent().child('date').val() || newData.val() >= newData.parent().child('time').val() )" }
        },
        "menuItems": {
          ".indexOn": ["creatorId"],
          // The second blanket permission, also removed: with it in place a
          // stranger could still wipe the whole menu of somebody else's event
          // in one write, and the per-item rule below would never be reached.
          "$itemId": {
            // Creating: the organizer always may. For everybody else the event
            // form's three switches are read as the three independent things
            // they are, because that is what they are on screen: offering a
            // ride answers to "allow ride offers", asking for one answers to
            // "allow ride requests", and everything else answers to "allow
            // participant items" plus the quota. Folding the rides inside the
            // item switch is what refused a ride in an event where the
            // organizer had switched items off and left rides on - and ride
            // offers are on by default, so that pairing is ordinary.
            // See DOCS/PLANING/29-rides-blocked-by-user-items-setting.md.
            //
            // A missing switch reads as off, the same way the event screen
            // reads it. A missing quota reads as three, the same default the
            // screens use.
            //
            // An ordinary item must arrive together with the writer's own
            // counter, stepped up by exactly one and still inside the quota.
            // Without that the quota only held against a client that chose to
            // report: an item written on its own read as "this person has never
            // added anything", every time, and eight of them went into an event
            // with a quota of three. The counter is read through newData so
            // that it is the value the same write is about to leave behind, not
            // the one already stored. Rides are exempt because they do not
            // count against the quota and so no longer carry a counter at all.
            //
            // Whoever the item says created it must be the one writing it, and
            // the field is frozen afterwards - checking it only on the way in
            // would let somebody create an item in their own name and then hand
            // it over by editing the field. The freeze is conditional on the
            // item still existing, or deleting one would read as changing it.
            // The organizer is exempt from both, and needs to be: bulk item
            // management records items under a fixed name, and turning a
            // participant's one-way ride into a round trip copies that
            // participant onto the new leg.
            // See DOCS/PLANING/30-item-rules-trust-the-client.md.
            //
            // Four category names count as a ride, matching RIDE_CATEGORY_IDS
            // in src/utils/eventUtils.ts - `trempim` and `rides` are older
            // names still present in existing events. This list and that one
            // had drifted apart, so an item in the name only one of them knew
            // was exempt from the quota on screen and counted against it here.
            // See DOCS/PLANING/31-rides-consume-the-item-quota.md.
            //
            // A ride now carries a counter of its own, stepped up exactly the
            // way the item counter is, which is what keeps an ordinary item
            // from taking the ride exemption without limit. Two offers and two
            // requests each, and no more, because a round trip is two items:
            // the ride form writes the outward leg and the return leg as two
            // separate rides in two separate writes, so a ceiling of one would
            // have created the first leg and refused the second.
            // Defined under rideOfferCounts and rideRequestCounts below.
            //
            // Editing and deleting: the organizer, or whoever created the item.
            // An item may not cross the line between a ride and an ordinary
            // item, in either direction, unless the organizer moves it.
            //
            // That was a third way round the quota and it was found while the
            // other two were being closed. A ride never touches the item
            // counter, so an item created as a ride and then edited into an
            // ordinary category is an item that never paid for its place, and
            // nothing here asked. The organizer stays exempt because bulk item
            // management re-categorises items as a matter of course, and
            // changing one ordinary category for another is untouched, which is
            // the correction people actually make.
            // See DOCS/PLANING/66-a-ride-can-be-edited-into-an-ordinary-item.md.
            ".write": "auth != null && ((!data.exists() && (root.child('events').child($eventId).child('organizerId').val() === auth.uid || (newData.child('creatorId').val() === auth.uid && ((!root.child('events').child($eventId).child('itemCount').exists() && newData.parent().parent().child('itemCount').val() === 1) || (root.child('events').child($eventId).child('itemCount').exists() && newData.parent().parent().child('itemCount').val() === root.child('events').child($eventId).child('itemCount').val() + 1 && newData.parent().parent().child('itemCount').val() <= 120)) && (((newData.child('category').val() === 'ride_offers' || newData.child('category').val() === 'trempim' || newData.child('category').val() === 'rides') && root.child('events').child($eventId).child('details').child('allowRideOffers').val() === true && ((!root.child('events').child($eventId).child('rideOfferCounts').child(auth.uid).exists() && newData.parent().parent().child('rideOfferCounts').child(auth.uid).val() === 1) || (root.child('events').child($eventId).child('rideOfferCounts').child(auth.uid).exists() && newData.parent().parent().child('rideOfferCounts').child(auth.uid).val() === root.child('events').child($eventId).child('rideOfferCounts').child(auth.uid).val() + 1 && newData.parent().parent().child('rideOfferCounts').child(auth.uid).val() <= 2))) || (newData.child('category').val() === 'ride_requests' && root.child('events').child($eventId).child('details').child('allowRideRequests').val() === true && ((!root.child('events').child($eventId).child('rideRequestCounts').child(auth.uid).exists() && newData.parent().parent().child('rideRequestCounts').child(auth.uid).val() === 1) || (root.child('events').child($eventId).child('rideRequestCounts').child(auth.uid).exists() && newData.parent().parent().child('rideRequestCounts').child(auth.uid).val() === root.child('events').child($eventId).child('rideRequestCounts').child(auth.uid).val() + 1 && newData.parent().parent().child('rideRequestCounts').child(auth.uid).val() <= 2))) || (root.child('events').child($eventId).child('details').child('allowUserItems').val() === true && ((!root.child('events').child($eventId).child('userItemCounts').child(auth.uid).exists() && newData.parent().parent().child('userItemCounts').child(auth.uid).val() === 1 && 1 <= (root.child('events').child($eventId).child('details').child('userItemLimit').exists() ? root.child('events').child($eventId).child('details').child('userItemLimit').val() : 3)) || (root.child('events').child($eventId).child('userItemCounts').child(auth.uid).exists() && newData.parent().parent().child('userItemCounts').child(auth.uid).val() === root.child('events').child($eventId).child('userItemCounts').child(auth.uid).val() + 1 && newData.parent().parent().child('userItemCounts').child(auth.uid).val() <= (root.child('events').child($eventId).child('details').child('userItemLimit').exists() ? root.child('events').child($eventId).child('details').child('userItemLimit').val() : 3)))))))) || (data.exists() && (root.child('events').child($eventId).child('organizerId').val() === auth.uid || (data.child('creatorId').val() === auth.uid && ((!newData.exists() && (!root.child('events').child($eventId).child('itemCount').exists() || newData.parent().parent().child('itemCount').val() === root.child('events').child($eventId).child('itemCount').val() - 1)) || (newData.child('creatorId').val() === data.child('creatorId').val() && (data.child('category').val() === 'ride_offers' || data.child('category').val() === 'trempim' || data.child('category').val() === 'rides' || data.child('category').val() === 'ride_requests') === (newData.child('category').val() === 'ride_offers' || newData.child('category').val() === 'trempim' || newData.child('category').val() === 'rides' || newData.child('category').val() === 'ride_requests')))))))",
            "name": { ".validate": "newData.isString() && newData.val().length <= 50" },
            "notes": { ".validate": "newData.isString() && newData.val().length <= 50" },
            "creatorName": { ".validate": "newData.isString() && newData.val().length <= 40" },
            "pickupLocation": { ".validate": "newData.isString() && newData.val().length <= 100" },
            "phoneNumber": { ".validate": "newData.isString() && newData.val().length <= 30" },
            // Taking an item and giving it back. This is where the protection
            // against two people grabbing the same item now lives: the field
            // may be filled only while it is empty, and only with your own
            // name, so the server refuses the second person no matter what the
            // app does. Releasing is allowed only to whoever is holding it.
            "assignedTo": {
              ".write": "auth != null && ( (!data.exists() && newData.val() === auth.uid) || (data.val() === auth.uid && (!newData.exists() || newData.val() === auth.uid)) )"
            },
            // The label and the timestamp follow whoever holds the item, so
            // they may be written by the holder either before or after the
            // change - which covers taking it, giving it back, and renaming.
            "assignedToName": {
              ".validate": "newData.isString() && newData.val().length <= 40",
              ".write": "auth != null && ( newData.parent().child('assignedTo').val() === auth.uid || data.parent().child('assignedTo').val() === auth.uid )"
            },
            "assignedAt": {
              ".write": "auth != null && ( newData.parent().child('assignedTo').val() === auth.uid || data.parent().child('assignedTo').val() === auth.uid )"
            }
          }
        },
        "participants": {
          "$participantId": {
            ".write": "auth != null && $participantId === auth.uid",
            "name": { ".validate": "newData.isString() && newData.val().length <= 40" }
          }
        },
        "assignments": {
          "$assignmentId": {
            // Your own sign-up, or the organizer removing one. The last clause
            // is new: deleting an item takes its sign-ups with it, and the
            // screen lets whoever created the item do that - it even counts the
            // people signed up in its confirmation.
            "userName": { ".validate": "newData.isString() && newData.val().length <= 40" },
            "notes": { ".validate": "newData.isString() && newData.val().length <= 50" },
            "phoneNumber": { ".validate": "newData.isString() && newData.val().length <= 30" },
            ".write": "auth != null && ( (newData.exists() && newData.child('userId').val() === auth.uid) || (data.exists() && data.child('userId').val() === auth.uid) || (data.exists() && !newData.exists() && root.child('events').child($eventId).child('organizerId').val() === auth.uid) || (data.exists() && !newData.exists() && root.child('events').child($eventId).child('menuItems').child(data.child('menuItemId').val()).child('creatorId').val() === auth.uid) )"
          }
        },
        "userItemCounts": {
          // Your own counter only, and only one step at a time, which is what
          // makes adding an item a compare-and-set.
          //
          // The two halves are kept apart on purpose. The arithmetic runs only
          // once there is a previous value to do arithmetic on: with the two
          // folded into one condition, a participant who has no counter yet -
          // every visitor arriving through an invitation link - was refused,
          // because the failed subtraction took the whole condition down before
          // the clause written to cover them was ever reached. That refusal took
          // the item down with it, since both are sent as one write.
          // See DOCS/PLANING/28-visitor-cannot-add-own-item.md.
          //
          // A first counter may only be one. Anything else would let a client
          // open at any number and step straight past the quota.
          //
          // Going up is free; coming down has to be paid for with a deletion.
          // The counter used to accept a step in either direction without
          // asking what the step was for, so a participant at three out of
          // three walked it back to zero in three legal writes and carried on
          // adding. Ten items went into an event whose quota is three.
          //
          // A rule cannot see which item a write deleted, but it can be told.
          // The write names the item it is removing under itemRemovals, and the
          // step down is allowed only when that item was this person's, was not
          // a ride, and is gone once the write lands. Naming an item that is
          // not being deleted buys nothing, and a name cannot be used twice
          // because the second time the item is no longer there to vouch for it.
          //
          // Rides are excluded because a ride never raised the counter. Letting
          // one lower it would hand out a free place on every ride deleted,
          // which is the same reasoning the product's own delete already uses.
          //
          // And the floor. It closes nothing on its own - that was measured -
          // but without it the counter goes negative and the quota opens with no
          // round trip at all.
          // See DOCS/PLANING/64-item-quota-can-be-walked-around.md.
          "$uid": {
            ".write": "auth != null && auth.uid === $uid && ( (!data.exists() && newData.val() === 1) || (data.exists() && newData.val() === data.val() + 1) || (data.exists() && newData.val() === data.val() - 1 && newData.val() >= 0 && root.child('events').child($eventId).child('menuItems').child(newData.parent().parent().child('itemRemovals').child($uid).val()).child('creatorId').val() === auth.uid && root.child('events').child($eventId).child('menuItems').child(newData.parent().parent().child('itemRemovals').child($uid).val()).child('category').val() !== 'ride_offers' && root.child('events').child($eventId).child('menuItems').child(newData.parent().parent().child('itemRemovals').child($uid).val()).child('category').val() !== 'ride_requests' && root.child('events').child($eventId).child('menuItems').child(newData.parent().parent().child('itemRemovals').child($uid).val()).child('category').val() !== 'trempim' && root.child('events').child($eventId).child('menuItems').child(newData.parent().parent().child('itemRemovals').child($uid).val()).child('category').val() !== 'rides' && !newData.parent().parent().child('menuItems').child(newData.parent().parent().child('itemRemovals').child($uid).val()).exists()) )"
          }
        },
        // Each of the four nodes below is writable by any signed-in person, so
        // each of them asks first whether the event is actually there. Without
        // that, every one would be another way to plant a node under events at
        // an id of the writer's choosing, which is the half of record 20 that
        // is still open. They are only ever written alongside an item in an
        // event that already exists, so the check costs nothing.
        // See DOCS/PLANING/20-anyone-can-create-an-event-node.md.
        //
        // Which item the write above is paying with. It carries no meaning on
        // its own and nothing reads it: it exists so that the counter rule has
        // something to check the deletion against. Writing it freely is
        // harmless, because it is only ever believed when the item it names
        // really was yours and really is gone.
        // How many items one event may hold.
        //
        // The per-participant quota does not bound this and cannot. The app
        // hands a fresh identity to every visitor who opens an invitation link,
        // so three items each is three items per identity, and identities are
        // free. This is the counter that makes the total finite.
        //
        // It binds participants exactly. It does not bind the organizer at all,
        // and that is the rule language rather than a choice: the migration
        // writes the whole menu in one go and nothing here can count what
        // arrives in it. The ceiling for the organizer is held in their own
        // screens instead. What the validate rule below does hold, against
        // everybody, is that the number itself never claims to be more than the
        // ceiling.
        //
        // Coming down is tied to a deletion exactly as the item quota is, and
        // it has to be: without that, adding and deleting in turn would leave
        // the count climbing on every round and would eventually shut the event
        // to everybody. An event that has no counter yet keeps working, which
        // is every event that existed before this shipped.
        // See DOCS/PLANING/57-central-limits-policy.md.
        "itemCount": {
          ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 120",
          ".write": "auth != null && root.child('events').child($eventId).exists() && ( (!data.exists() && newData.val() === 1) || (data.exists() && newData.val() === data.val() + 1) || (data.exists() && newData.val() === data.val() - 1 && root.child('events').child($eventId).child('menuItems').child(newData.parent().child('itemRemovals').child(auth.uid).val()).child('creatorId').val() === auth.uid && !newData.parent().child('menuItems').child(newData.parent().child('itemRemovals').child(auth.uid).val()).exists()) )"
        },
        "itemRemovals": {
          "$uid": {
            ".write": "auth != null && root.child('events').child($eventId).exists() && auth.uid === $uid"
          }
        },
        // One ride each way, offered and asked for, per person, per event.
        //
        // Rides are exempt from the item quota on purpose, because offering a
        // lift is not something anybody brings to the meal. Nothing checked
        // that an item claiming the exemption was a ride, so an ordinary item
        // with a ride category written on it was exempt outright: ten in a row
        // were accepted at a full quota.
        //
        // Requiring the category to be one the organizer defined was the first
        // proposal and it cannot be built. The event's category list is stored
        // as an array, which the rule language cannot search, and the ride
        // categories are added on screen and never stored at all - so every
        // genuine ride in a default event would have been refused.
        //
        // What bounds it instead is the promise the event screen already makes
        // and the server never kept, that one person offers one lift and asks
        // for one. The ceiling is two rather than one because a round trip is
        // two items: the ride form writes the outward leg and the return leg as
        // two separate rides, so a ceiling of one would have created the first
        // and refused the second, leaving half a round trip behind.
        //
        // These are counters and not markers on purpose, so that they behave
        // exactly like the item counter above and come down under exactly the
        // same condition: the write names the ride it is deleting, and that
        // ride has to have been this person's, to have been a ride of this
        // kind, and to be gone once the write lands. The exemption is worth
        // four items at the very most instead of an unlimited number.
        // See DOCS/PLANING/64-item-quota-can-be-walked-around.md
        // and DOCS/PLANING/31-rides-consume-the-item-quota.md.
        "rideOfferCounts": {
          "$uid": {
            ".write": "auth != null && root.child('events').child($eventId).exists() && auth.uid === $uid && ( (!data.exists() && newData.val() === 1) || (data.exists() && newData.val() === data.val() + 1 && newData.val() <= 2) || (data.exists() && newData.val() === data.val() - 1 && newData.val() >= 0 && root.child('events').child($eventId).child('menuItems').child(newData.parent().parent().child('itemRemovals').child($uid).val()).child('creatorId').val() === auth.uid && (root.child('events').child($eventId).child('menuItems').child(newData.parent().parent().child('itemRemovals').child($uid).val()).child('category').val() === 'ride_offers' || root.child('events').child($eventId).child('menuItems').child(newData.parent().parent().child('itemRemovals').child($uid).val()).child('category').val() === 'trempim' || root.child('events').child($eventId).child('menuItems').child(newData.parent().parent().child('itemRemovals').child($uid).val()).child('category').val() === 'rides') && !newData.parent().parent().child('menuItems').child(newData.parent().parent().child('itemRemovals').child($uid).val()).exists()) )"
          }
        },
        "rideRequestCounts": {
          "$uid": {
            ".write": "auth != null && root.child('events').child($eventId).exists() && auth.uid === $uid && ( (!data.exists() && newData.val() === 1) || (data.exists() && newData.val() === data.val() + 1 && newData.val() <= 2) || (data.exists() && newData.val() === data.val() - 1 && newData.val() >= 0 && root.child('events').child($eventId).child('menuItems').child(newData.parent().parent().child('itemRemovals').child($uid).val()).child('creatorId').val() === auth.uid && root.child('events').child($eventId).child('menuItems').child(newData.parent().parent().child('itemRemovals').child($uid).val()).child('category').val() === 'ride_requests' && !newData.parent().parent().child('menuItems').child(newData.parent().parent().child('itemRemovals').child($uid).val()).exists()) )"
          }
        }
      }
    },
    // Legacy node. Live preset lists moved to users/$uid/presetLists; one
    // abandoned record remains here. Restricted to its owner.
    "presetLists": {
      "$listId": {
        ".read": "auth != null && data.child('createdBy').val() === auth.uid",
        ".write": "auth != null && ( !data.exists() || data.child('createdBy').val() === auth.uid ) && ( !newData.exists() || newData.child('createdBy').val() === auth.uid )"
      }
    },
    "admins": {
      // Was ".read": true - readable by anyone on the internet, unauthenticated.
      // The node is currently empty and has no reader in live code; the first
      // admin is still added through the console, as before.
      ".read": "auth != null && root.child('admins').child(auth.uid).exists()",
      "$uid": {
        ".write": "root.child('admins').child(auth.uid).exists()"
      }
    }
  }
}
